diff --git a/docs/kotlin/multiplatform/sqlite.md b/docs/kotlin/multiplatform/sqlite.md
index 53cd8b95..97a7b797 100644
--- a/docs/kotlin/multiplatform/sqlite.md
+++ b/docs/kotlin/multiplatform/sqlite.md
@@ -12,11 +12,11 @@ harnessing the full power of SQLite.
 
 ## Set up dependencies
 
-To setup SQLite in your KMP project, add the dependencies for the artifacts in
+To set up SQLite in your KMP project, add the dependencies for the artifacts in
 the `build.gradle.kts` file for your module:
 
 > [!NOTE]
-> **Note:** `androidx.sqlite` supports KMP in version 2.5.0 or higher.
+> **Note:** `androidx.sqlite` supports KMP in version 2.5.0 and higher. Web support, including JavaScript and WasmJS, requires version 2.7.0 and higher.
 
     [versions]
     sqlite = "2.7.0"
@@ -39,11 +39,11 @@ the SQLite library either included in the library when using
 when using `androidx.sqlite:sqlite-framework`. The APIs closely follow the core
 functionality of SQLite C API.
 
-There are 3 main interfaces:
+There are three main interfaces:
 
-- [`SQLiteDriver`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteDriver) - It is the entry point to use SQLite and is responsible for opening database connections.
-- [`SQLiteConnection`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteConnection) - Is the representation of the `sqlite3` object.
-- [`SQLiteStatement`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteStatement) - Is the representation of the `sqlite3_stmt` object.
+- [`SQLiteDriver`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteDriver): The entry point to SQLite, used to open database connections.
+- [`SQLiteConnection`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteConnection): Represents the `sqlite3` object.
+- [`SQLiteStatement`](https://developer.android.com/reference/kotlin/androidx/sqlite/SQLiteStatement): Represents the `sqlite3_stmt` object.
 
 The following example showcases the core APIs:
 
@@ -67,17 +67,17 @@ The following example showcases the core APIs:
       databaseConnection.close()
     }
 
-Similar to SQLite C APIs the common usage is to:
+Similar to the SQLite C APIs, the common usage pattern consists of these steps:
 
-- Open a database connection using the instantiated `SQLiteDriver` implementation.
-- Prepare a SQL statement using `SQLiteConnection.prepare()`
-- Execute a `SQLiteStatement` in the following way:
-  1. Optionally bind arguments using the `bind*()` functions.
-  2. Iterate over the result set using the `step()` function.
-  3. Read columns from the result set using the `get*()` functions.
+1. Open a database connection using the instantiated `SQLiteDriver` implementation.
+2. Prepare a SQL statement using `SQLiteConnection.prepare`.
+3. Execute a `SQLiteStatement` by doing the following:
+   1. Optional: Bind arguments using the `bind*` functions.
+   2. Iterate over the result set using the `step` function.
+   3. Read columns from the result set using the `get*` functions.
 
 > [!CAUTION]
-> **Caution:** Database connections and statements are resources that need to be managed. All prepared statements should be closed once they are no longer needed. Similarly if a database connection is no longer needed, then it should be closed.
+> **Caution:** Database connections and statements are resources that you must manage. You must close all prepared statements when you no longer need them. Similarly, if you no longer need a database connection, you must close it.
 
 ### Driver Implementations
 
@@ -88,6 +88,7 @@ The following table summarizes the available driver implementations:
 | `AndroidSQLiteDriver` | `androidx.sqlite:sqlite-framework` | Android |
 | `NativeSQLiteDriver` | `androidx.sqlite:sqlite-framework` | iOS, Mac, and Linux |
 | `BundledSQLiteDriver` | `androidx.sqlite:sqlite-bundled` | Android, iOS, Mac, Linux and JVM (Desktop) |
+| `WebWorkerSQLiteDriver` | `androidx.sqlite:sqlite-web` | JavaScript and WebAssembly (WasmJS) |
 
 The recommended implementation to use is `BundledSQLiteDriver` available in
 `androidx.sqlite:sqlite-bundled`. It includes the SQLite library compiled from
@@ -97,21 +98,21 @@ supported KMP platforms.
 ### SQLite Driver and Room
 
 The driver APIs are useful for low-level interactions with an SQLite database.
-For a feature rich library that provides a more robust access of SQLite then
-Room is recommended.
-
-A `RoomDatabase` relies on a `SQLiteDriver` to perform database operations and
-an implementation is required to be configured using
-[`RoomDatabase.Builder.setDriver()`](https://developer.android.com/reference/kotlin/androidx/room/RoomDatabase#setDriver(androidx.sqlite.SQLiteDriver)). Room provides
-[`RoomDatabase.useReaderConnection`](https://developer.android.com/reference/kotlin/androidx/room/package-summary#(androidx.room.RoomDatabase).useReaderConnection(kotlin.coroutines.SuspendFunction1)) and
-[`RoomDatabase.useWriterConnection`](https://developer.android.com/reference/kotlin/androidx/room/package-summary#(androidx.room.RoomDatabase).useWriterConnection(kotlin.coroutines.SuspendFunction1)) for more direct access to the managed
+For a feature-rich library that provides more robust access to SQLite, we
+recommend Room.
+
+A `RoomDatabase` relies on a `SQLiteDriver` to perform database operations,
+and you must configure an implementation using
+[`RoomDatabase.Builder.setDriver`](https://developer.android.com/reference/kotlin/androidx/room3/RoomDatabase#setDriver(androidx.sqlite.SQLiteDriver)). Room provides
+[`RoomDatabase.useReaderConnection`](https://developer.android.com/reference/kotlin/androidx/room3/package-summary#(androidx.room3.RoomDatabase).useReaderConnection(kotlin.coroutines.SuspendFunction1)) and
+[`RoomDatabase.useWriterConnection`](https://developer.android.com/reference/kotlin/androidx/room3/package-summary#(androidx.room3.RoomDatabase).useWriterConnection(kotlin.coroutines.SuspendFunction1)) for more direct access to the managed
 database connections.
 
 ## Migrate to Kotlin Multiplatform
 
-Any usage of low-level Support SQLite API components (such as the
-`SupportSQLiteDatabase` interface) needs to be migrated to the equivalent
-SQLite Driver components.
+You must migrate any use of low-level Support SQLite API components,
+such as the `SupportSQLiteDatabase` interface, to the equivalent SQLite
+Driver components.
 
 ### Kotlin Multiplatform
 
@@ -181,7 +182,7 @@ Execute a query with result but no arguments
 
     val database: SupportSQLiteDatabase = ...
     database.query("SELECT * FROM Pet").use { cursor ->
-      while (cusor.moveToNext()) {
+      while (cursor.moveToNext()) {
         // read columns
         cursor.getInt(0)
         cursor.getString(1)
