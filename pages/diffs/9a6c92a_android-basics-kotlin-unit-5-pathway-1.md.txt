diff --git a/docs/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1.md b/docs/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1.md
index 078f6b5..9e66006 100644
--- a/docs/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1.md
+++ b/docs/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1.md
@@ -1,90 +1,99 @@
 ---
-title: https://developer.android.com/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1
+title: Introduction to SQL, Room, and Flow  |  Android Developers
 url: https://developer.android.com/courses/quizzes/android-basics-kotlin-unit-5-pathway-1/android-basics-kotlin-unit-5-pathway-1
-source: md.txt
+source: html-scrape
 ---
 
-# Introduction to SQL, Room, and Flow
+Stay organized with collections
+
+Save and categorize content based on your preferences.
+
+
 
 # Introduction to SQL, Room, and Flow
 
-<br />
+Return to pathway
+
+
+
+1. ## True or False: `INSERT`, `UPDATE`, and `DELETE` can be used to modify multiple rows in a data table.
+
+   True
+
+   False
+2. ## Which of the following are true about the `DISTINCT` keyword?
+
+   Choose as many answers as you see fit.
+
+   The `DISTINCT` keyword can be used in an aggregate function.
+
+   The `DISTINCT` keyword can be used to determine all the unique values in a column.
+
+   The `DISTINCT` keyword can only be used when selecting a single column.
+
+   The `DISTINCT` keyword can be used as part of the `WHERE` clause.
+3. ## Which order of clauses in a `SELECT` statement is correct?
+
+   `WHERE`, `LIMIT`, `ORDER BY`, `GROUP BY`
+
+   `WHERE`, `LIMIT`, `GROUP BY`, `ORDER BY`
+
+   `WHERE`, `ORDER BY`, `GROUP BY`, `LIMIT`
+
+   `WHERE`, `GROUP BY`, `ORDER BY`, `LIMIT`
+4. ## True or False: The `ORDER BY` clause returns results in descending order by default.
+
+   True
 
-Return to pathway  
-1.
+   False
+5. ## Fill-in-the-blanks
 
-   ## True or False:`INSERT`,`UPDATE`, and`DELETE`can be used to modify multiple rows in a data table.
+   Enter one or more words to complete the sentence.
 
-   True  
-   False  
-2.
+   SQL stands for \_\_\_.
+6. ## True or False: You can use the `*` to select all columns in a data table
 
-   ## Which of the following are true about the`DISTINCT`keyword?
+   True
 
-   Choose as many answers as you see fit.  
-   The`DISTINCT`keyword can be used in an aggregate function.  
-   The`DISTINCT`keyword can be used to determine all the unique values in a column.  
-   The`DISTINCT`keyword can only be used when selecting a single column.  
-   The`DISTINCT`keyword can be used as part of the`WHERE`clause.  
-3.
+   False
+7. ## A primary key allows for:
 
-   ## Which order of clauses in a`SELECT`statement is correct?
+   Choose as many answers as you see fit.
 
-   `WHERE`,`LIMIT`,`ORDER BY`,`GROUP BY`  
-   `WHERE`,`LIMIT`,`GROUP BY`,`ORDER BY`  
-   `WHERE`,`ORDER BY`,`GROUP BY`,`LIMIT`  
-   `WHERE`,`GROUP BY`,`ORDER BY`,`LIMIT`  
-4.
+   Databases to represent relationships between tables.
 
-   ## True or False: The`ORDER BY`clause returns results in descending order by default.
+   Rows in one table to reference rows in another table.
 
-   True  
-   False  
-5.
+   Contents of individual rows in a table to be unique.
 
-   ## Fill-in-the-blanks
+   A unique identifier for each row in a table.
+8. ## Fill-in-the-blanks
 
-   Enter one or more words to complete the sentence.  
-   SQL stands for ___.  
-6.
+   Enter one or more words to complete the sentence.
 
-   ## True or False: You can use the`*`to select all columns in a data table
+   Room is an ORM Library. ORM stands for \_\_\_.
+9. ## Which of the following are true about `ListAdapter`?
 
-   True  
-   False  
-7.
+   Choose as many answers as you see fit.
 
-   ## A primary key allows for:
+   A `ListAdapter` is not a `RecyclerViewAdapter`, and thus, cannot be used with a `RecyclerView`.
 
-   Choose as many answers as you see fit.  
-   Databases to represent relationships between tables.  
-   Rows in one table to reference rows in another table.  
-   Contents of individual rows in a table to be unique.  
-   A unique identifier for each row in a table.  
-8.
+   Like a `RecyclerViewAdapter`, a `ListAdapter` uses a `ViewHolder` class.
 
-   ## Fill-in-the-blanks
+   `ListAdapter` makes use of `DiffUtil`.
 
-   Enter one or more words to complete the sentence.  
-   Room is an ORM Library. ORM stands for ___.  
-9.
+   The implementation for `areContentsTheSame()` compares only the IDs between entities, whereas `areItemsTheSame()` compares all the properties.
+10. ## Which of the following is false about using flow?
 
-   ## Which of the following are true about`ListAdapter`?
+    Returning a regular type, like a list, from a DAO function won't allow your app to respond to data changes.
 
-   Choose as many answers as you see fit.  
-   A`ListAdapter`is not a`RecyclerViewAdapter`, and thus, cannot be used with a`RecyclerView`.  
-   Like a`RecyclerViewAdapter`, a`ListAdapter`uses a`ViewHolder`class.  
-   `ListAdapter`makes use of`DiffUtil`.  
-   The implementation for`areContentsTheSame()`compares only the IDs between entities, whereas`areItemsTheSame()`compares all the properties.  
-10.
+    Data returned as a flow can be processed using the collect() function.
 
-    ## Which of the following is false about using flow?
+    A flow can only be accessed from a coroutine.
 
-    Returning a regular type, like a list, from a DAO function won't allow your app to respond to data changes.  
-    Data returned as a flow can be processed using the collect() function.  
-    A flow can only be accessed from a coroutine.  
-    Flow is a Kotlin feature that returns multiple asynchronous computed values.  
+    Flow is a Kotlin feature that returns multiple asynchronous computed values.
 
 Submit answers
 
-*error_outline*An error occurred when grading the quiz. Please try again.
\ No newline at end of file
+*error\_outline*
+An error occurred when grading the quiz. Please try again.
\ No newline at end of file
