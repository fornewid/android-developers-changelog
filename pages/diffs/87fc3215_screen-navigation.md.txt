diff --git a/docs/training/cars/apps/library/screen-navigation.md b/docs/training/cars/apps/library/screen-navigation.md
index f58f737c..d2cdda4b 100644
--- a/docs/training/cars/apps/library/screen-navigation.md
+++ b/docs/training/cars/apps/library/screen-navigation.md
@@ -4,17 +4,13 @@ url: https://developer.android.com/training/cars/apps/library/screen-navigation
 source: md.txt
 ---
 
-Apps often present a number of different screens, each possibly using
-different templates the user can navigate through as they interact with
-the interface.
+Apps often present a number of different screens, each possibly using different templates the user can navigate through as they interact with the interface.
 
-The `ScreenManager` class provides a screen stack you can use to push screens
-that can be popped automatically when the user selects a **Back** button on the
-car screen or uses the hardware **Back** button available in some cars.
+The `ScreenManager` class provides a screen stack you can use to push screens that can be popped automatically when the user selects a **Back** button on the car screen or uses the hardware **Back** button available in some cars.
 
-This code shows how to add a back action to a message template as well as an
-action to push a new screen when selected by the user:
+This code shows how to add a back action to a message template as well as an action to push a new screen when selected by the user:
 
+<br />
 
 ```kotlin
 val header = Header.Builder()
@@ -30,13 +26,11 @@ val template = MessageTemplate.Builder("Hello world!")
             .build()
     )
     .build()
+   
 ```
 
 <br />
 
-The [`Action.BACK`](https://developer.android.com/reference/androidx/car/app/model/Action#BACK()) object is a standard [`Action`](https://developer.android.com/reference/androidx/car/app/model/Action) that automatically
-invokes [`ScreenManager.pop`](https://developer.android.com/reference/androidx/car/app/ScreenManager#pop()). This behavior can be overridden by using the
-[`OnBackPressedDispatcher`](https://developer.android.com/reference/androidx/activity/OnBackPressedDispatcher) instance available from the `CarContext`.
+The [`Action.BACK`](https://developer.android.com/reference/androidx/car/app/model/Action#BACK()) object is a standard [`Action`](https://developer.android.com/reference/androidx/car/app/model/Action) that automatically invokes [`ScreenManager.pop`](https://developer.android.com/reference/androidx/car/app/ScreenManager#pop()). This behavior can be overridden by using the [`OnBackPressedDispatcher`](https://developer.android.com/reference/androidx/activity/OnBackPressedDispatcher) instance available from the `CarContext`.
 
-To promote safe driving, the screen stack can consist of no more than five
-screens. To learn more, see [Template restrictions](https://developer.android.com/training/cars/apps/library/template-restrictions).
\ No newline at end of file
+To promote safe driving, the screen stack can consist of no more than five screens. To learn more, see [Template restrictions](https://developer.android.com/training/cars/apps/library/template-restrictions).
\ No newline at end of file
