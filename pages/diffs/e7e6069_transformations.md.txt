diff --git a/docs/media/media3/transformer/transformations.md b/docs/media/media3/transformer/transformations.md
index d542878..5633b67 100644
--- a/docs/media/media3/transformer/transformations.md
+++ b/docs/media/media3/transformer/transformations.md
@@ -1,22 +1,11 @@
 ---
-title: Transformations  |  Android media  |  Android Developers
+title: https://developer.android.com/media/media3/transformer/transformations
 url: https://developer.android.com/media/media3/transformer/transformations
-source: html-scrape
+source: md.txt
 ---
 
-Media3 Transformer is actively under development and we are looking to hear from you! We welcome your feedback, feature requests and bug reports in the [issue tracker](https://github.com/androidx/media/issues). Follow the [ExoPlayer blog](https://medium.com/google-exoplayer) for the latest updates.
-
-* [Android Developers](https://developer.android.com/)
-* [Essentials](https://developer.android.com/get-started)
-* [Media dev center](https://developer.android.com/media)
-* [Guides](https://developer.android.com/media/guides)
-
-# Transformations Stay organized with collections Save and categorize content based on your preferences.
-
-
-
 clips, apply video effects, and optimize exports.
-keywords\_public: Media3, Transformer, transcode, video editing, trimming, audio
+keywords_public: Media3, Transformer, transcode, video editing, trimming, audio
 processing, Android media
 
 ## Transcode between formats
@@ -25,28 +14,27 @@ You can specify the output audio and video formats you want to produce when
 building Transformer. For example, the following code shows how
 to configure Transformer to output H.264/AVC video and AAC audio:
 
+
 ### Kotlin
 
-```
+```kotlin
 Transformer.Builder(context)
   .setVideoMimeType(MimeTypes.VIDEO_H264)
   .setAudioMimeType(MimeTypes.AUDIO_AAC)
   .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new Transformer.Builder(context)
     .setVideoMimeType(MimeTypes.VIDEO_H264)
     .setAudioMimeType(MimeTypes.AUDIO_AAC)
     .build();
-
-Transformations.java
 ```
 
+<br />
+
 If the input media format already matches the configurations for audio
 or video, Transformer automatically switches to *transmuxing*, that is, copying
 the compressed samples from the input container to the output container without
@@ -57,31 +45,31 @@ decoding and re-encoding in the same format.
 
 Remove audio or video using `EditedMediaItem.Builder`, for example:
 
+
 ### Kotlin
 
-```
+```kotlin
 EditedMediaItem.Builder(inputMediaItem).setRemoveAudio(true).build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new EditedMediaItem.Builder(inputMediaItem).setRemoveAudio(true).build();
-
-Transformations.java
 ```
 
+<br />
+
 ## Trim a clip
 
 You can remove any media outside specified start and end timestamps by setting
 the clipping configuration on the input media item. For example, to produce a
 clip containing only the media between 10 seconds and 20 seconds:
 
+
 ### Kotlin
 
-```
+```kotlin
 val inputMediaItem =
   MediaItem.Builder()
     .setUri(uri)
@@ -92,13 +80,11 @@ val inputMediaItem =
         .build()
     )
     .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 MediaItem inputMediaItem =
     new MediaItem.Builder()
         .setUri(uri)
@@ -108,10 +94,10 @@ MediaItem inputMediaItem =
                 .setEndPositionMs(20_000)
                 .build())
         .build();
-
-Transformations.java
 ```
 
+<br />
+
 ## MP4 edit lists
 
 For faster trimming, Transformer supports MP4 edit lists, allowing for more
@@ -123,52 +109,45 @@ the undesired initial segment.
 To make trim-only edits significantly faster, call
 `experimentalSetMp4EditListTrimEnabled(true)`.
 
+
 ### Kotlin
 
-```
+```kotlin
 Transformer.Builder(context).experimentalSetMp4EditListTrimEnabled(true).build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new Transformer.Builder(context).experimentalSetMp4EditListTrimEnabled(true).build();
-
-Transformations.java
 ```
+It is important to note that not all media players support a "pre-roll" position. This means that when such a player is used, the file will start playback from the absolute beginning of the encoded sample, regardless of any edit list information that might specify a different starting point.
 
-It is important to note that not all media players support a "pre-roll"
-position. This means that when such a player is used, the file will start
-playback from the absolute beginning of the encoded sample, regardless of any
-edit list information that might specify a different starting point.
+<br />
 
-**Caution:** Trimming video often implies permanent removal of unwanted sections.
-However, using edit lists creates a privacy risk: users might unknowingly share
-sensitive, "deleted" information "hidden" by the "pre-roll" position.
+> [!CAUTION]
+> **Caution:** Trimming video often implies permanent removal of unwanted sections. However, using edit lists creates a privacy risk: users might unknowingly share sensitive, "deleted" information "hidden" by the "pre-roll" position.
 
 ## Optimizing trims
 
 To reduce the latency of trimming the beginning of a video, enable trim
 optimization.
 
+
 ### Kotlin
 
-```
+```kotlin
 Transformer.Builder(context).experimentalSetTrimOptimizationEnabled(true).build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new Transformer.Builder(context).experimentalSetTrimOptimizationEnabled(true).build();
-
-Transformations.java
 ```
 
+<br />
+
 This speeds up the export by decoding and re-encoding as little of the video as
 possible, then stitching the re-encoded data with the rest of the original
 video. The optimization relies on being able to stitch part of the input file
@@ -198,9 +177,10 @@ You can rescale media, which can be useful to save on processing resources or
 bandwidth when dealing with very high resolution input, such as 4k or 8k video.
 For example, to scale proportionally to 480 pixels high:
 
+
 ### Kotlin
 
-```
+```kotlin
 EditedMediaItem.Builder(MediaItem.fromUri(uri))
   .setEffects(
     Effects(
@@ -209,28 +189,27 @@ EditedMediaItem.Builder(MediaItem.fromUri(uri))
     )
   )
   .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new EditedMediaItem.Builder(MediaItem.fromUri(uri))
     .setEffects(
         new Effects(
             /* audioProcessors= */ ImmutableList.of(),
             /* videoEffects= */ ImmutableList.of(Presentation.createForHeight(480))))
     .build();
-
-Transformations.java
 ```
 
+<br />
+
 Alternatively, you can scale by a given factor, for example, to halve the size:
 
+
 ### Kotlin
 
-```
+```kotlin
 val editedMediaItem =
   EditedMediaItem.Builder(MediaItem.fromUri(uri))
     .setEffects(
@@ -242,13 +221,11 @@ val editedMediaItem =
       )
     )
     .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new EditedMediaItem.Builder(MediaItem.fromUri(uri))
     .setEffects(
         new Effects(
@@ -256,15 +233,16 @@ new EditedMediaItem.Builder(MediaItem.fromUri(uri))
             /* videoEffects= */ ImmutableList.of(
                 new ScaleAndRotateTransformation.Builder().setScale(.5f, .5f).build())))
     .build();
-
-Transformations.java
 ```
 
+<br />
+
 You can configure rotation in the same way:
 
+
 ### Kotlin
 
-```
+```kotlin
 EditedMediaItem.Builder(MediaItem.fromUri(uri))
   .setEffects(
     Effects(
@@ -275,13 +253,11 @@ EditedMediaItem.Builder(MediaItem.fromUri(uri))
     )
   )
   .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 new EditedMediaItem.Builder(MediaItem.fromUri(uri))
     .setEffects(
         new Effects(
@@ -289,10 +265,10 @@ new EditedMediaItem.Builder(MediaItem.fromUri(uri))
             /* videoEffects= */ ImmutableList.of(
                 new ScaleAndRotateTransformation.Builder().setRotationDegrees(90f).build())))
     .build();
-
-Transformations.java
 ```
 
+<br />
+
 ### Custom video effects
 
 The `Effects` constructor accepts a list of audio and video effects to apply.
@@ -306,25 +282,27 @@ Video effects are also supported for preview in ExoPlayer, using
 `ExoPlayer.setVideoEffects`. For an example of how to use this API, check out
 the [effect demo app](https://github.com/androidx/media/tree/release/demos/effect).
 
-The [demo app](/media/media3/transformer/demo-application) includes examples of custom video effects.
+The [demo app](https://developer.android.com/media/media3/transformer/demo-application) includes examples of custom video effects.
 
 ## Image input
 
 Transformer supports image inputs by treating them as static video clips. To
 configure an image as an input source, follow these steps:
 
-* Create a `MediaItem` using `MediaItem.Builder`. Specify the display
+- Create a `MediaItem` using `MediaItem.Builder`. Specify the display
   duration of the image in the output video by calling `setImageDurationMs`.
-* Construct an `EditedMediaItem` wrapping the `MediaItem`. Specify the target
+
+- Construct an `EditedMediaItem` wrapping the `MediaItem`. Specify the target
   frame rate for the generated video stream using
   `EditedMediaItem.Builder#setFrameRate`.
 
 The following example demonstrates how to configure an image input to generate a
 5-second video at 30 frames per second:
 
+
 ### Kotlin
 
-```
+```kotlin
 val imageMediaItem =
   MediaItem.Builder()
     .setUri(imageUri)
@@ -335,13 +313,11 @@ val editedImageItem =
   EditedMediaItem.Builder(imageMediaItem)
     .setFrameRate(30) // 30 frames per second
     .build()
-
-Transformations.kt
 ```
 
 ### Java
 
-```
+```java
 MediaItem imageMediaItem =
     new MediaItem.Builder()
         .setUri(imageUri)
@@ -350,10 +326,10 @@ MediaItem imageMediaItem =
 new EditedMediaItem.Builder(imageMediaItem)
     .setFrameRate(30) // 30 frames per second
     .build();
-
-Transformations.java
 ```
 
+<br />
+
 ## Audio edits
 
 Audio effects are implemented by applying a sequence of `AudioProcessor`
