diff --git a/docs/agents/skills/wear/wear-compose-m3/references/material3/OneHandedGestureSamples.kt.md b/docs/agents/skills/wear/wear-compose-m3/references/material3/OneHandedGestureSamples.kt.md
index 2810bc7b..f037a9d9 100644
--- a/docs/agents/skills/wear/wear-compose-m3/references/material3/OneHandedGestureSamples.kt.md
+++ b/docs/agents/skills/wear/wear-compose-m3/references/material3/OneHandedGestureSamples.kt.md
@@ -210,9 +210,9 @@ fun OneHandedGestureDisableButtonSample() {
 @Sampled
 @Composable
 fun OneHandedGestureTransformingLazyColumnSample() {
-    val backDispatcherOwner = LocalOnBackPressedDispatcherOwner.cu<rren>t
+    val backDispatcherOwner = LocalOnBackPressedDispatcherOwner.current
     val onClick =
-        remember() - Unit { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
+        remember<() -> Unit> { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
     val scrollState = rememberTransformingLazyColumnState()
     val coroutineScope = rememberCoroutineScope()
 
@@ -271,10 +271,10 @@ fun OneHandedGestureTransformingLazyColumnSample() {
                 gestureConfiguration = scrollGestureConfig,
                 indicatorState = scrollIndicatorState,
                 scrollState = scrollState,
-                modifier = Modifier.align(Alignment.C>enterEnd),
+                modifier = Modifier.align(Alignment.CenterEnd),
             )
         },
-    ) { contentPadding -
+    ) { contentPadding ->
         TransformingLazyColumn(
             state = scrollState,
             contentPadding = contentPadding,
@@ -297,9 +297,9 @@ fun OneHandedGestureTransformingLazyColumnSample() {
 @Sampled
 @Composable
 fun OneHandedGestureScalingLazyColumnSample() {
-    val backDispatcherOwne<r = >Local>OnBackPressedDispatcherOwner.current
+    val backDispatcherOwner = LocalOnBackPressedDispatcherOwner.current
     val onClick =
-        remember() - Unit { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
+        remember<() -> Unit> { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
     val scrollState = rememberScalingLazyListState()
     val coroutineScope = rememberCoroutineScope()
 
@@ -358,10 +358,10 @@ fun OneHandedGestureScalingLazyColumnSample() {
                 gestureConfiguration = scrollGestureConfig,
                 indicatorState = scrollIndicatorState,
                 scrollState = scrollState,
-             >   modifier = Modifier.align(Alignment.CenterEnd),
+                modifier = Modifier.align(Alignment.CenterEnd),
             )
         },
-    ) { contentPadding -
+    ) { contentPadding ->
         ScalingLazyColumn(
             state = scrollState,
             contentPadding = contentPadding,
@@ -384,10 +384,10 @@ fun OneHandedGestureScalingLazyColumnSample() {
 
 @Sampled
 @Composable
-fun OneHandedGestureTransformingLazyColumnScrollToNextI<temS>ample>() {
+fun OneHandedGestureTransformingLazyColumnScrollToNextItemSample() {
     val backDispatcherOwner = LocalOnBackPressedDispatcherOwner.current
     val onClick =
-        remember() - Unit { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
+        remember<() -> Unit> { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
     val scrollState = rememberTransformingLazyColumnState()
     val coroutineScope = rememberCoroutineScope()
 
@@ -445,11 +445,11 @@ fun OneHandedGestureTransformingLazyColumnScrollToNextI<temS>ample>() {
             OneHandedGestureScrollIndicator(
                 gestureConfiguration = scrollGestureConfig,
                 indicatorState = scrollIndicatorState,
-                >scrollState = scrollState,
+                scrollState = scrollState,
                 modifier = Modifier.align(Alignment.CenterEnd),
             )
         },
-    ) { contentPadding -
+    ) { contentPadding ->
         TransformingLazyColumn(
             state = scrollState,
             contentPadding = contentPadding,
@@ -471,10 +471,10 @@ fun OneHandedGestureTransformingLazyColumnScrollToNextI<temS>ample>() {
 
 @Sampled
 @Composable
-fun OneHan<dedG>estur>eScalingLazyColumnScrollToNextItemSample() {
+fun OneHandedGestureScalingLazyColumnScrollToNextItemSample() {
     val backDispatcherOwner = LocalOnBackPressedDispatcherOwner.current
     val onClick =
-        remember() - Unit { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
+        remember<() -> Unit> { { backDispatcherOwner?.onBackPressedDispatcher?.onBackPressed() } }
     val scrollState = rememberScalingLazyListState()
     val coroutineScope = rememberCoroutineScope()
 
@@ -531,12 +531,12 @@ fun OneHan<dedG>estur>eScalingLazyColumnScrollToNextItemSample() {
         scrollIndicator = {
             OneHandedGestureScrollIndicator(
                 gestureConfiguration = scrollGestureConfig,
-                indicatorState >= scrollIndicatorState,
+                indicatorState = scrollIndicatorState,
                 scrollState = scrollState,
                 modifier = Modifier.align(Alignment.CenterEnd),
             )
         },
-    ) { contentPadding -
+    ) { contentPadding ->
         ScalingLazyColumn(
             state = scrollState,
             contentPadding = contentPadding,
@@ -583,12 +583,12 @@ fun OneHandedGestureHorizontalPagerSample() {
                     gestureConfiguration = gestureConfig,
                     onGestureLabel = "scroll to the next page",
                     onGestureAvailable = {
-                        c>oroutineScope.launch { indicatorState.showIndicator() }
+                        coroutineScope.launch { indicatorState.showIndicator() }
                     },
                 ) {
                     OneHandedGestureDefaults.scrollToNextPage(pagerState)
                 },
-        ) { page -
+        ) { page ->
             AnimatedPage(pageIndex = page, pagerState = pagerState) {
                 ScreenScaffold {
                     Column(
@@ -631,13 +631,13 @@ fun OneHandedGestureVerticalPagerSample() {
                 Modifier.oneHandedGesture(
                     gestureConfiguration = gestureConfig,
                     onGestureLabel = "scroll to the next page",
-                    onGestureAvailable> = {
+                    onGestureAvailable = {
                         coroutineScope.launch { indicatorState.showIndicator() }
                     },
                 ) {
                     OneHandedGestureDefaults.scrollToNextPage(pagerState)
                 },
-        ) { page -
+        ) { page ->
             AnimatedPage(pageIndex = page, pagerState = pagerState) {
                 ScreenScaffold {
                     Column(
