diff --git a/docs/agents/skills/wear/wear-compose-m3/references/material3/SwipeToRevealSample.kt.md b/docs/agents/skills/wear/wear-compose-m3/references/material3/SwipeToRevealSample.kt.md
index cef26c08..655e188a 100644
--- a/docs/agents/skills/wear/wear-compose-m3/references/material3/SwipeToRevealSample.kt.md
+++ b/docs/agents/skills/wear/wear-compose-m3/references/material3/SwipeToRevealSample.kt.md
@@ -76,7 +76,8 @@ fun SwipeToRevealSample() {
                 text = { Text("Delete") },
             )
         },
-        onSwipePrimaryAction = { /* This block is called when the full swipe gesture is performed. */
+        onSwipePrimaryAction = {
+            /* This block is called when the full swipe gesture is performed. */
         },
         secondaryAction = {
             SecondaryActionButton(
@@ -126,7 +127,8 @@ fun SwipeToRevealSingleActionCardSample() {
                 modifier = Modifier.height(SwipeToRevealDefaults.LargeActionButtonHeight),
             )
         },
-        onSwipePrimaryAction = { /* This block is called when the full swipe gesture is performed. */
+        onSwipePrimaryAction = {
+            /* This block is called when the full swipe gesture is performed. */
         },
         undoPrimaryAction = {
             UndoActionButton(
@@ -285,7 +287,8 @@ fun SwipeToRevealWithScalingLazyColumnSample() {
                 onSwipePrimaryAction = { messages.remove(message) },
                 secondaryAction = {
                     SecondaryActionButton(
-                        onClick = { /* This block is called when the secondary action is executed. */
+                        onClick = {
+                            /* This block is called when the secondary action is executed. */
                         },
                         icon = { Icon(Icons.Outlined.MoreVert, contentDescription = "Options") },
                     )
