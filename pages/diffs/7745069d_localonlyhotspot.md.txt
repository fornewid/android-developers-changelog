diff --git a/docs/develop/connectivity/wifi/localonlyhotspot.md b/docs/develop/connectivity/wifi/localonlyhotspot.md
index 8d0cdfe9..cd29568b 100644
--- a/docs/develop/connectivity/wifi/localonlyhotspot.md
+++ b/docs/develop/connectivity/wifi/localonlyhotspot.md
@@ -4,9 +4,20 @@ url: https://developer.android.com/develop/connectivity/wifi/localonlyhotspot
 source: md.txt
 ---
 
-You can use a local-only hotspot to enable applications on devices connected to the Wi-Fi hotspot to communicate with each other. The network created by this method will not have Internet access. Each application can make a single request for the hotspot, but multiple applications can request the hotspot at the same time. When multiple applications have successfully registered concurrently, they share the underlying hotspot. [`LocalOnlyHotspotCallback.onStarted(LocalOnlyHotspotReservation)`](https://developer.android.com/reference/android/net/wifi/WifiManager.LocalOnlyHotspotCallback#onStarted(android.net.wifi.WifiManager.LocalOnlyHotspotReservation)) is called when the hotspot is ready for use.
+You can use a local-only hotspot to enable applications on devices connected to
+the Wi-Fi hotspot to communicate with each other. The network created by this
+method will not have Internet access. Each application can make a single request
+for the hotspot, but multiple applications can request the hotspot at the same
+time. When multiple applications have successfully registered concurrently, they
+share the underlying hotspot.
+[`LocalOnlyHotspotCallback.onStarted(LocalOnlyHotspotReservation)`](https://developer.android.com/reference/android/net/wifi/WifiManager.LocalOnlyHotspotCallback#onStarted(android.net.wifi.WifiManager.LocalOnlyHotspotReservation))
+is called when the hotspot is ready for use.
 
-If your app targets Android 13 (API level 33) or higher, you must request the [`NEARBY_WIFI_DEVICES`](https://developer.android.com/reference/android/Manifest.permission#NEARBY_WIFI_DEVICES) to use a local-only hotspot, as shown in the following code snippet. Apps that target an earlier version of Android must request `ACCESS_FINE_LOCATION` instead.
+If your app targets Android 13 (API level 33) or higher, you must request the
+[`NEARBY_WIFI_DEVICES`](https://developer.android.com/reference/android/Manifest.permission#NEARBY_WIFI_DEVICES)
+to use a local-only hotspot, as shown in the following code snippet. Apps that
+target an earlier version of Android must request `ACCESS_FINE_LOCATION`
+instead.
 
 ```xml
 <manifest ...>
@@ -28,4 +39,5 @@ If your app targets Android 13 (API level 33) or higher, you must request the [`
 </manifest>
 ```
 
-For more details on using local-only hotspots, see [`startLocalOnlyHotspot()`](https://developer.android.com/reference/android/net/wifi/WifiManager#startLocalOnlyHotspot(android.net.wifi.WifiManager.LocalOnlyHotspotCallback,%20android.os.Handler)).
\ No newline at end of file
+For more details on using local-only hotspots, see
+[`startLocalOnlyHotspot()`](https://developer.android.com/reference/android/net/wifi/WifiManager#startLocalOnlyHotspot(android.net.wifi.WifiManager.LocalOnlyHotspotCallback,%20android.os.Handler)).
\ No newline at end of file
