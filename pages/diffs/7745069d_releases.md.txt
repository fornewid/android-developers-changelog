diff --git a/docs/topic/generic-system-image/releases.md b/docs/topic/generic-system-image/releases.md
index e3cbcf1e..67ea80ed 100644
--- a/docs/topic/generic-system-image/releases.md
+++ b/docs/topic/generic-system-image/releases.md
@@ -6,17 +6,29 @@ source: md.txt
 
 ![](https://developer.android.com/static/images/lockups/android-stacked.svg)
 
-Android Generic System Images (GSIs) are for app developers to perform app validation and for development purposes. You shouldn't redistribute GSIs or use them in any other way except as specifically set forth in the license terms enclosed in each individual download.
+Android Generic System Images (GSIs) are for app developers to perform app
+validation and for development purposes. You shouldn't redistribute GSIs or use
+them in any other way except as specifically set forth in the license terms
+enclosed in each individual download.
 
-For more information about GSI device requirements, risks, and installation instructions, refer to the [GSI documentation page](https://developer.android.com/topic/generic-system-image).
+For more information about GSI device requirements, risks, and installation
+instructions, refer to the [GSI documentation page](https://developer.android.com/topic/generic-system-image).
 
 ## Install with Android Flash Tool
 
-**Android Flash Tool** lets you securely flash a system image to your supported Pixel device. Android Flash Tool works with any Web browser that supports WebUSB, such as Chrome or Edge 79+.
+**Android Flash Tool** lets you securely flash a system image
+to your supported Pixel device. Android Flash Tool works with any Web browser
+that supports WebUSB, such as Chrome or Edge 79+.
 
-Android Flash Tool guides you step-by-step through the process of flashing your device---there's no need to have tools installed---but you do need to unlock your device and [enable USB Debugging in Developer options](https://developer.android.com/studio/debug/dev-options#enable). For complete instructions, see the [Android Flash Tool documentation](https://source.android.com/setup/contribute/flash).
+Android Flash Tool guides you step-by-step through the process of flashing your
+device---there's no need to have tools installed---but you do need to unlock your
+device and [enable USB Debugging in Developer options](https://developer.android.com/studio/debug/dev-options#enable). For
+complete instructions, see the [Android Flash Tool
+documentation](https://source.android.com/setup/contribute/flash).
 
-Connect your device over USB, then, depending on the type of system image you want to flash, navigate to Android Flash Tool using one of the following links and follow the onscreen guidance:
+Connect your device over USB, then, depending on the type of system image you
+want to flash, navigate to Android Flash Tool using one of the following links
+and follow the onscreen guidance:
 
 - Android 17 QPR2 (Beta)
   - **ARM64 GSI with GMS** : <https://flash.android.com/preview/cinnamonbun-qpr2-beta2-gsi-gms>
@@ -64,7 +76,10 @@ See the following sections.
 
 ### Android 17 QPR2 (Beta)
 
-GSI binaries for Android 17 QPR2 Beta are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/qpr2/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 17 QPR2 Beta are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/qpr2/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6a
 - Pixel 7
@@ -98,7 +113,6 @@ GSI binaries for Android 17 QPR2 Beta are built from the same AOSP and GMS sourc
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a17_qpr2_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-CP41.260717.006-15938186-14256f39.zip</button> `14256f396bb0ba71290035b7520a5b9373d5bc19c9b9876076bfaeda70997680` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a17_qpr2_aosp_x86_64_GSI_zip">aosp_x86_64-exp-CP41.260717.006-15938186-84ddb26a.zip</button> `84ddb26a555ce59e89da311043461c4155543508fd2f26e1b57318fa4707aea7` |
 
-<br />
 
 ### Download Android 17 GSI Release
 
@@ -654,7 +668,10 @@ See the following sections.
 
 ### Android 17 QPR1 (Beta)
 
-GSI binaries for Android 17 QPR1 Beta are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/qpr1/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 17 QPR1 Beta are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/qpr1/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6
 - Pixel 6 Pro
@@ -690,7 +707,6 @@ GSI binaries for Android 17 QPR1 Beta are built from the same AOSP and GMS sourc
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a17_qpr1_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-CP31.260623.010-15961508-0e96757d.zip</button> `0e96757db88e1dea46f39945aebee513d5a512b79cfa7091e11c7014a011725d` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a17_qpr1_aosp_x86_64_GSI_zip">aosp_x86_64-exp-CP31.260623.010-15961508-5dcb9866.zip</button> `5dcb9866fe554db2431c605dbf8fb61fa3cb8e579a22f008b6c6df574f02366a` |
 
-<br />
 
 ### Download Android 17 GSI Release
 
@@ -1246,7 +1262,10 @@ See the following sections.
 
 ### Android 17
 
-GSI binaries for Android 17 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 17 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/17/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6
 - Pixel 6 Pro
@@ -1282,7 +1301,6 @@ GSI binaries for Android 17 are built from the same AOSP and GMS sources as the
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a17_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-CP2A.260605.012-15430684-d12aaa6e.zip</button> `d12aaa6ec8e330c755788efb307c22596ffc5f2f3c5a4bdd79ec4040a7884274` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a17_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-CP2A.260605.012-15430684-3da2ca55.zip</button> `3da2ca5599d304c4d92fe8c72f31d035a59db57c93ce073740ed4015ee034a8e` |
 
-<br />
 
 ### Download Android 17 GSI Release
 
@@ -1838,7 +1856,10 @@ See the following sections.
 
 ### Android 16 QPR3 (Beta)
 
-GSI binaries for Android 16 QPR3 Beta are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 16 QPR3 Beta are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 6a
@@ -1863,7 +1884,6 @@ GSI binaries for Android 16 QPR3 Beta are built from the same AOSP and GMS sourc
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a16_qpr3_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-CP11.251209.009.A1-14840729-37289f85.zip</button> `37289f85ddaebb1ce1147bdb75e9d47053737e71946833f524977f492b55e035` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a16_qpr3_aosp_x86_64_GSI_zip">aosp_x86_64-exp-CP11.251209.009.A1-14840729-f96f7906.zip</button> `f96f7906d9638b8242c20fb9651ed322937ac5195ffa2ae3df01393ad089e594` |
 
-<br />
 
 ### Download Android 16 GSI Release
 
@@ -2415,7 +2435,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 16 QPR2
 
-GSI binaries for Android 16 QPR2 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 16 QPR2 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 6a
@@ -2440,7 +2463,6 @@ GSI binaries for Android 16 QPR2 are built from the same AOSP and GMS sources as
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a16_qpr2_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-BP4A.251205.006-14401865-b41f700e.zip</button> `b41f700ef3b9d1479cdbabb73b0a4862cf91fcd9471d08cfdb7e3c7a81f37f29` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a16_qpr2_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-BP4A.251205.006-14401865-aa5d832e.zip</button> `aa5d832edfb7e4c05138b459b21a219039048f65bc103ac38e25e410fa561512` |
 
-<br />
 
 ### Download Android 16 GSI Release
 
@@ -2992,7 +3014,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 16 QPR1
 
-GSI binaries for Android 16 QPR1 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 16 QPR1 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 6a
@@ -3017,7 +3042,6 @@ GSI binaries for Android 16 QPR1 are built from the same AOSP and GMS sources as
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a16_qpr1_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-BP3A.250905.014-13873947-7566270d.zip</button> `7566270d0150753cdf9d3f798286e24da03176542a82faf48500e34c335ad806` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a16_qpr1_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-BP3A.250905.014-13873947-9485bc07.zip</button> `9485bc07ee64e8bcdaa28de08b04b420f484ae1605fcb65ad4eca57efb9a9606` |
 
-<br />
 
 ### Download Android 16 GSI Release
 
@@ -3569,7 +3593,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 16 (initial release)
 
-GSI binaries for Android 16 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 16 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/16/get).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 6a
@@ -3594,7 +3621,6 @@ GSI binaries for Android 16 are built from the same AOSP and GMS sources as the
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a16_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-BP2A.250605.031.A3-13578795-ccf343cd.zip</button> `ccf343cd5f0261ca4d3fcc442ad45a5cdedc05a10a7056427e5381013a0b04d4` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a16_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-BP2A.250605.031.A3-13578795-47b1a5bc.zip</button> `47b1a5bc29f31ffb451e9bb96cf30ce200498d5aa4258d75ec8b80a22adaf0e5` |
 
-<br />
 
 ### Download Android 16 GSI Release
 
@@ -4146,10 +4172,12 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 16 GSIs
 
-Android 16 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 16 GSI binaries have the following GSI-specific known issues that might
+occur with some devices and builds:
 
 - **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 15 GSIs
 
@@ -4157,7 +4185,10 @@ See the following sections.
 
 ### Android 15 QPR2
 
-GSI binaries for Android 15 QPR2 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get-qpr). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 15 QPR2 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get-qpr).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 7 and 7 Pro
@@ -4180,7 +4211,6 @@ GSI binaries for Android 15 QPR2 are built from the same AOSP and GMS sources as
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a15_qpr2_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-BP1A.250405.005.C1-13151952-a080bdd6.zip</button> `a080bdd66ea02e0f46189cbb3fec542c4ae00881d03948f73306c86a1e1df479` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a15_qpr2_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-BP1A.250405.005.C1-13151952-12978bf9.zip</button> `12978bf94c1e8dfee598941641df2aa73d29a0f967cc00f4f36b87ac1ed77bf2` |
 
-<br />
 
 ### Download Android 15 GSI Release
 
@@ -4732,7 +4762,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 15 QPR1
 
-GSI binaries for Android 15 QPR1 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get-qpr1). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 15 QPR1 are built from the same AOSP and GMS
+sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get-qpr1).
+These binaries contain the same API and SDK, have a similar CTS result, and have
+been validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 7 and 7 Pro
@@ -4755,7 +4788,6 @@ GSI binaries for Android 15 QPR1 are built from the same AOSP and GMS sources as
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a15_qpr1_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-AP4A.241205.013-12621605-32b57682.zip</button> `32b57682319b2695c96ddce37bdae25bcf2ea11bbfe5688692a5a8bda953b868` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a15_qpr1_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-AP4A.241205.013-12621605-422944cb.zip</button> `422944cbefde67e232c1acb486d27c7f72ce63954a58b48141f7bb2b37baccf7` |
 
-<br />
 
 ### Download Android 15 GSI Release
 
@@ -5307,7 +5339,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 15 (initial release)
 
-GSI binaries for Android 15 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 15 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/15/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 6 and 6 Pro
 - Pixel 7 and 7 Pro
@@ -5330,7 +5365,6 @@ GSI binaries for Android 15 are built from the same AOSP and GMS sources as the
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a15_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-AP3A.241005.015-12366759-d77cb747.zip</button> `d77cb747f9a901178498b0dc63269c6d09fbbc863cd8d66513ea7a8148a95fe8` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a15_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-AP3A.241005.015-12366759-96716f9b.zip</button> `96716f9bff890f7336274b8bc80b3dd816b3571f881beffbfe91800ee41300f9` |
 
-<br />
 
 ### Download Android 15 GSI Release
 
@@ -5882,13 +5916,22 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 15 GSIs
 
-Android 15 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 15 GSI binaries have the following GSI-specific known issues that might
+occur with some devices and builds:
 
-- **Bluetooth audio**: In some cases, Bluetooth audio can't be heard after pairing a new device.
+- **Bluetooth audio**: In some cases, Bluetooth audio can't be heard after
+  pairing a new device.
 
-- **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
+- **Power Cycle**: Rebooting GSI might fail on some devices. To work around
+  it, reboot the device into recovery mode, erase user data, perform a factory
+  reset, and then reboot the device.
 
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named
+  `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system
+  partition size on your device. To work around this issue, you can delete
+  some non-essential dynamic partitions, such as the product partition, and
+  flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 14 GSIs
 
@@ -5896,7 +5939,10 @@ See the following sections.
 
 ### Android 14 QPR1
 
-GSI binaries for Android 14 QPR1 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/14/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 14 QPR1 are built from the same AOSP and GMS sources as
+the [corresponding Google Pixel builds](https://developer.android.com/about/versions/14/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 5a
 - Pixel 6 and 6 Pro
@@ -5919,7 +5965,6 @@ GSI binaries for Android 14 QPR1 are built from the same AOSP and GMS sources as
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a14_qpr1_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-UQ1A.231205.015-11084887-5f53f7cc.zip</button> `5f53f7cc53e58357e57dbe46e475e1c50429d7e4ab9ba41be88de041a44960b9` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a14_qpr1_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-UQ1A.231205.015-11084887-f8788769.zip</button> `f8788769ea2497d555bd46334f9faa81697ed538cdaaacce68d72471f56cb8c5` |
 
-<br />
 
 ### Download Android 14 GSI Release
 
@@ -6471,7 +6516,10 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Android 14 (initial, stable release)
 
-GSI binaries for Android 14 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/14/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 14 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/14/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 4a (5G)
 - Pixel 5 and 5a
@@ -6495,7 +6543,6 @@ GSI binaries for Android 14 are built from the same AOSP and GMS sources as the
 | x86_64+GMS | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64-gms" data-modal-dialog-id="a14_stable_gms_x86_64_GSI_zip">gsi_gms_x86_64-exp-UP1A.231005.007-10754064-a42e6bb7.zip</button> `a42e6bb7b1267bb756f87db5d36fd285a20de2b6943d6233be689144052fd788` |
 | x86_64 | <button class="devsite-dialog-button button-white button-regular gc-analytics-event" data-category="gsi-release" data-action="download" data-label="x86_64" data-modal-dialog-id="a14_stable_aosp_x86_64_GSI_zip">aosp_x86_64-exp-UP1A.231005.007-10754064-84570261.zip</button> `845702612585350234877b416c7f03ded1095832b2fe5ae4e9cc8a741d33cb71` |
 
-<br />
 
 ### Download Android 14 GSI Release
 
@@ -7047,14 +7094,19 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 14 GSIs
 
-Android 14 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 14 GSI binaries have the following GSI-specific known issues that might
+occur with some devices and builds:
 
 - **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 13 GSIs
 
-GSI binaries for Android 13 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/13/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 13 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/13/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 4 and 4 XL
 - Pixel 4a and 4a (5G)
@@ -8190,14 +8242,19 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 13 GSIs
 
-Android 13 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 13 GSI binaries have the following GSI-specific known issues that might
+occur with some devices and builds:
 
 - **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 12 GSIs
 
-GSI binaries for Android 12 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/12/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 12 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/12/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 3 and 3 XL
 - Pixel 3a and 3a XL
@@ -8768,17 +8825,30 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 12 GSIs
 
-Android 12 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 12 GSI binaries have the following GSI-specific known issues that might
+occur with some devices and builds:
 
-- **Phone Audio**: When using the integrated dialer, you might not hear any audio on the phone. This is due to a change in the telephony service installation location in Android 10.
+- **Phone Audio**: When using the integrated dialer, you might not hear any
+  audio on the phone. This is due to a change in the telephony service
+  installation location in Android 10.
 
-- **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
+- **Power Cycle**: Rebooting GSI might fail on some devices. To work around
+  it, reboot the device into recovery mode, erase user data, perform a factory
+  reset, and then reboot the device.
 
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named
+  `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system
+  partition size on your device. To work around this issue, you can delete
+  some non-essential dynamic partitions, such as the product partition, and
+  flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 11 GSIs
 
-GSI binaries for Android 12 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/11/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 12 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/11/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel 2 and 2 XL
 - Pixel 3 and 3 XL
@@ -9347,17 +9417,30 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 11 GSIs
 
-Android 11 GSI binaries have the following GSI-specific known issues that might occur with some devices and builds:
+Android 11 GSI binaries have the following GSI-specific known
+issues that might occur with some devices and builds:
 
-- **Phone Audio**: When using the integrated dialer, you might not hear any audio on the phone. This is due to a change in the telephony service installation location in Android 10.
+- **Phone Audio**: When using the integrated dialer, you might not hear any
+  audio on the phone. This is due to a change in the telephony service
+  installation location in Android 10.
 
-- **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
+- **Power Cycle**: Rebooting GSI might fail on some devices. To work around
+  it, reboot the device into recovery mode, erase user data, perform a factory
+  reset, and then reboot the device.
 
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
+- **System partition size** : GSI + GMS file size (images named
+  `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system
+  partition size on your device. To work around this issue, you can delete
+  some non-essential dynamic partitions, such as the product partition, and
+  flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
 
 ## Android 10 GSIs
 
-GSI binaries for Android 10 are built from the same AOSP and GMS sources as the [corresponding Google Pixel builds](https://developer.android.com/about/versions/10/get#on_pixel). These binaries contain the same API and SDK, have a similar CTS result, and have been validated on the following Pixel devices:
+GSI binaries for Android 10 are built from the same AOSP and GMS sources as the
+[corresponding Google Pixel builds](https://developer.android.com/about/versions/10/get#on_pixel). These
+binaries contain the same API and SDK, have a similar CTS result, and have been
+validated on the following Pixel devices:
 
 - Pixel and Pixel XL
 - Pixel 2 and 2 XL
@@ -9925,10 +10008,20 @@ I have read and agree with the above terms and conditions <button class="button
 
 ### Known issues with Android 10 GSIs
 
-This version of the Android 10 GSI has the following known issues that might occur with some devices and builds:
+This version of the Android 10 GSI has the following known issues that might
+occur with some devices and builds:
 
-- **Phone Audio**: When using the integrated dialer, you might not hear any audio on the phone. This is due to a change in the telephony service installation location in Android 10.
+- **Phone Audio**: When using the integrated dialer, you might not hear any
+  audio on the phone. This is due to a change in the telephony service
+  installation location in Android 10.
 
-- **Power Cycle**: Rebooting GSI might fail on some devices. To work around it, reboot the device into recovery mode, erase user data, perform a factory reset, and then reboot the device.
+- **Power Cycle**: Rebooting GSI might fail on some devices. To work around
+  it, reboot the device into recovery mode, erase user data, perform a factory
+  reset, and then reboot the device.
 
-- **System partition size** : GSI + GMS file size (images named `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system partition size on your device. To work around this issue, you can delete some non-essential dynamic partitions, such as the product partition, and flash the GSI again. For more information, see the [flashing GSIs documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
\ No newline at end of file
+- **System partition size** : GSI + GMS file size (images named
+  `_gsi\_gms\_arm64-*_`) might be bigger than the default dynamic system
+  partition size on your device. To work around this issue, you can delete
+  some non-essential dynamic partitions, such as the product partition, and
+  flash the GSI again. For more information, see the [flashing GSIs
+  documentation](https://source.android.com/setup/build/gsi#flashing-gsis).
\ No newline at end of file
