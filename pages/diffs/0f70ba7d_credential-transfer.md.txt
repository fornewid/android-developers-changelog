diff --git a/docs/identity/sign-in/credential-transfer.md b/docs/identity/sign-in/credential-transfer.md
index f74334a8..0fb78b02 100644
--- a/docs/identity/sign-in/credential-transfer.md
+++ b/docs/identity/sign-in/credential-transfer.md
@@ -112,14 +112,21 @@ suspend fun registerMyProviderForExport(
 
 When the user selects your `ExportEntry` on the system selector UI, the system
 launches your designated handling `Activity`. Declare this activity with the
-mandatory intent action and content URI scheme:
+mandatory intent action and content URI scheme.
+
+To protect against unauthorized invocations and confused-deputy attacks, declare
+`android:requireContentUriPermissionFromCaller="write"` on Android 14 (API level
+34) and higher. This instructs the Android operating system to verify that the
+caller initiating the launch holds write permissions to the destination content
+URI before allowing the activity to start, as shown in the following example:
 
     <activity
         android:name="com.example.CredentialExportActivity"
         android:exported="true"
+        android:requireContentUriPermissionFromCaller="write"
         android:label="@string/export_activity_label">
         <intent-filter>
-            // This intent action is required for Credential Manager to invoke this activity
+            <!-- This intent action is required for Credential Manager to invoke this activity -->
             <action android:name="androidx.identitycredentials.action.IMPORT_CREDENTIALS" />
             <category android:name="android.intent.category.DEFAULT" />
             <data android:scheme="content" />
@@ -130,11 +137,18 @@ mandatory intent action and content URI scheme:
 
 In your `CredentialExportActivity`, use
 `IntentHandler.retrieveProviderImportCredentialsRequest(intent)` to parse the
-request, verify the calling app and `credId`, perform any required biometric
-authentication, and write the FIDO CXF payload back to the provided content URI:
+request, verify the calling component and request metadata, perform any
+required biometric authentication, and write the FIDO CXF payload back to the
+provided content URI.
 
-> [!NOTE]
-> **Note:** In this sample, `isTrustedImporter` returns `true` as a placeholder. In a production application, you should implement actual verification logic to ensure the calling app is authorized to receive the credentials.
+#### Authenticate the request
+
+Because your activity is exported, protect the credential export flow by
+verifying each of the following layers:
+
+1. **Verify the `credId` session nonce (primary defense):** When registering your `ExportEntry`, generate a cryptographically random, high-entropy `id` (nonce) and associate it with a short-lived session. In your activity, verify that `request.credId` matches an active, unexpired session nonce previously issued by your provider. Because third-party apps can't guess this nonce, this ensures that the intent corresponds to an authentic user selection on the system selector.
+2. **Verify `Activity.getCallingPackage()` (caller check):** Because the credential selector launches your activity using `startActivityForResult`, your activity can inspect `getCallingPackage()` to confirm that the immediate caller launching the UI is the trusted system credential selector framework (on Android devices with Google Play services, `"com.google.android.gms"`). Reject the request if the calling package is unrecognized.
+3. **Inspect `request.callingAppInfo` (target app information):** The `callingAppInfo` identifies the destination application that will receive the imported credentials. You can use this information as you see fit for transparency (such as displaying the destination app name and icon in your confirmation UI) or verifying whether the destination application is trusted.
 
 
 ```kotlin
