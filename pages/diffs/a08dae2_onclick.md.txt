diff --git a/docs/training/constraint-layout/motionlayout/ref/onclick.md b/docs/training/constraint-layout/motionlayout/ref/onclick.md
index c6b05a3..214cf68 100644
--- a/docs/training/constraint-layout/motionlayout/ref/onclick.md
+++ b/docs/training/constraint-layout/motionlayout/ref/onclick.md
@@ -4,9 +4,13 @@ url: https://developer.android.com/training/constraint-layout/motionlayout/ref/o
 source: md.txt
 ---
 
-# &lt;OnClick&gt;
+[Video](https://www.youtube.com/watch?v=CiFuElrEhKc)
 
-Specifies the action to perform when the user taps on a view. There can be multiple`<OnClick>`nodes for a single[`<Transition>`](https://developer.android.com/training/constraint-layout/motionlayout/ref/transition), with each`<OnClick>`specifying a different target view and a different action to perform when the view is tapped.
+Specifies the action to perform when the user taps on a view. There can be
+multiple `<OnClick>` nodes for a single
+[`<Transition>`](https://developer.android.com/training/constraint-layout/motionlayout/ref/transition), with
+each `<OnClick>` specifying a different target view and a different action to
+perform when the view is tapped.
 
 ## Syntax
 
@@ -19,7 +23,9 @@ Specifies the action to perform when the user taps on a view. There can be multi
 ## Attributes
 
 `motion:targetId`
-:   View being monitored. When the user taps this view, the transition occurs.
+:   View being monitored. When the user taps this view, the transition
+    occurs.
 
 `motion:ClickAction`
-:   Action to perform when the view is tapped. Supported values are the following:
\ No newline at end of file
+:   Action to perform when the view is tapped. Supported values are the
+    following:
\ No newline at end of file
