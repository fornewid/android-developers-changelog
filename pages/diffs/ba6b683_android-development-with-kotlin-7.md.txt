diff --git a/docs/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7.md b/docs/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7.md
index c8971c3..66c1251 100644
--- a/docs/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7.md
+++ b/docs/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7.md
@@ -1,73 +1,63 @@
 ---
-title: Lesson 7: Activity and fragment lifecycles  |  Android Developers
+title: https://developer.android.com/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7
 url: https://developer.android.com/courses/quizzes/android-development-with-kotlin-7/android-development-with-kotlin-7
-source: html-scrape
+source: md.txt
 ---
 
-Stay organized with collections
-
-Save and categorize content based on your preferences.
-
-
-
 # Lesson 7: Activity and fragment lifecycles
 
-Return to pathway
-
-
-
-1. ## Your app contains a physics simulation that requires heavy computation to display. Then the user gets a phone call. Which of the following is true?
-
-   During the phone call, you should continue computing the positions of objects in the physics simulation.
-
-   During the phone call, you should stop computing the positions of objects in the physics simulation.
-2. ## Which lifecycle method should you override to pause the simulation when the app is not on the screen?
-
-   `onDestroy()`
-
-   `onStop()`
-
-   `onPause()`
-
-   `onSaveInstanceState()`
-3. ## To make a class lifecycle-aware through the Android lifecycle library, which interface should the class implement?
-
-   `Lifecycle`
-
-   `LifecycleOwner`
+# Lesson 7: Activity and fragment lifecycles
 
-   `Lifecycle.Event`
+<br />
 
-   `LifecycleObserver`
-4. ## Under which circumstances does the `onCreate()` method in your activity receive a `Bundle` with data in it (that is, the `Bundle` is not `null`)?
+Return to pathway  
+1.
 
-   The activity is restarted after the device is rotated.
+   ## Your app contains a physics simulation that requires heavy computation to display. Then the user gets a phone call. Which of the following is true?
 
-   The activity is started from scratch.
+   During the phone call, you should continue computing the positions of objects in the physics simulation.  
+   During the phone call, you should stop computing the positions of objects in the physics simulation.  
+2.
 
-   The activity is resumed after returning from the background.
+   ## Which lifecycle method should you override to pause the simulation when the app is not on the screen?
 
-   The device is rebooted.
-5. ## Which lifecycle method is called to make an activity visible?
+   `onDestroy()`  
+   `onStop()`  
+   `onPause()`  
+   `onSaveInstanceState()`  
+3.
 
-   `onPause()`
+   ## To make a class lifecycle-aware through the Android lifecycle library, which interface should the class implement?
 
-   `onVisible()`
+   `Lifecycle`  
+   `LifecycleOwner`  
+   `Lifecycle.Event`  
+   `LifecycleObserver`  
+4.
 
-   `onStart()`
+   ## Under which circumstances does the`onCreate()`method in your activity receive a`Bundle`with data in it (that is, the`Bundle`is not`null`)?
 
-   `onDestroy()`
-6. ## Which lifecycle method is called to give an activity focus?
+   The activity is restarted after the device is rotated.  
+   The activity is started from scratch.  
+   The activity is resumed after returning from the background.  
+   The device is rebooted.  
+5.
 
-   `onResume()`
+   ## Which lifecycle method is called to make an activity visible?
 
-   `onVisible()`
+   `onPause()`  
+   `onVisible()`  
+   `onStart()`  
+   `onDestroy()`  
+6.
 
-   `onStart()`
+   ## Which lifecycle method is called to give an activity focus?
 
-   `onFocus()`
+   `onResume()`  
+   `onVisible()`  
+   `onStart()`  
+   `onFocus()`  
 
 Submit answers
 
-*error\_outline*
-An error occurred when grading the quiz. Please try again.
\ No newline at end of file
+*error_outline*An error occurred when grading the quiz. Please try again.
\ No newline at end of file
