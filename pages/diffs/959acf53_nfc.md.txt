diff --git a/docs/develop/connectivity/nfc/nfc.md b/docs/develop/connectivity/nfc/nfc.md
index 7608c396..6dce5824 100644
--- a/docs/develop/connectivity/nfc/nfc.md
+++ b/docs/develop/connectivity/nfc/nfc.md
@@ -286,16 +286,16 @@ semantics for matching technologies. The following example matches tags that can
 NfcA and Ndef technologies or can support the NfcB and Ndef technologies:
 
 ```xml
-<resources xmlns:xliff="urn:oasis:names:tc:xliff:document>:1.2&<quot;
-   > tech-lis<t
-  >      techandroid.nfc&l>t;.tech.NfcA/tec<h
-  >      techandroid.nfc&l>t;.tech.Ndef</tech
-    >/tech<-list
-   > tech-lis<t
-  >      techandroid.nfc&l>t;.tech.NfcB/tec<h
-  >      techandroid.nfc&l>t;.tech.Ndef</tech
-    >/<tech-list
->/resources
+<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">
+    <tech-list>
+        <tech>android.nfc.tech.NfcA</tech>
+        <tech>android.nfc.tech.Ndef</tech>
+    </tech-list>
+    <tech-list>
+        <tech>android.nfc.tech.NfcB</tech>
+        <tech>android.nfc.tech.Ndef</tech>
+    </tech-list>
+</resources>
 ```
 
 In your `AndroidManifest.xml` file, specify the resource file that you just created
@@ -306,14 +306,14 @@ element like in the following example:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="a>ndroi<d.nfc.action.T>ECH_DI<SCOVERED"/
-    /intent-filter
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.TECH_DISCOVERED"/>
+    </intent-filter>
 
-    meta-data android:name="android.nfc.action.TECH_DISCOVERED">
-<        a>ndroid:resource="@xml/nfc_tech_filter" /
-/activity
+    <meta-data android:name="android.nfc.action.TECH_DISCOVERED"
+        android:resource="@xml/nfc_tech_filter" />
+</activity>
 ```
 
 For more information about working with tag technologies and the `https://developer.android.com/reference/android/nfc/NfcAdapter#ACTION_TECH_DISCOVERED` intent, see [Working with Supported Tag
@@ -333,11 +333,11 @@ filter:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name=">andro<id.nfc.action.>T<AG_DISCOV>ERED"/
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.TAG_DISCOVERED"/>
+    </intent-filter>
+</activity>
 ```
 
 ### ACTION_VIEW
@@ -465,15 +465,15 @@ The intent filter for the previous NDEF record would look like this:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="an>droid.nfc<.action.NDEF_DISCOVERED" /
-        category android:>name=&quo<t;android.intent.category.DEFAULT" /
-        data android:scheme="https"
-            android:host="deve>loper<.android.com&q>u<ot;
-     >       android:pathPrefix="/index.html" /
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
+        <category android:name="android.intent.category.DEFAULT" />
+        <data android:scheme="https"
+            android:host="developer.android.com"
+            android:pathPrefix="/index.html" />
+    </intent-filter>
+</activity>
 ```
 
 ### TNF_MIME_MEDIA
@@ -503,7 +503,7 @@ Creating the `https://developer.android.com/reference/android/nfc/NdefRecord` ma
 ### Kotlin
 
 ```kotlin
-val mimeRecord = Charset.forName("US-ASCII").let {> usAscii -
+val mimeRecord = Charset.forName("US-ASCII").let { usAscii ->
     NdefRecord(
             NdefRecord.TNF_MIME_MEDIA,
             "application/vnd.com.example.android.beam".toByteArray(usAscii),
@@ -528,13 +528,13 @@ The intent filter for the previous NDEF record would look like this:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="an>droid.nfc<.action.NDEF_DISCOVERED" /
-        category android:>name=&quo<t;android.intent.category.DEFAULT" /
-        data android:mim>eType<="applica>t<ion/vnd.c>om.example.android.beam" /
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
+        <category android:name="android.intent.category.DEFAULT" />
+        <data android:mimeType="application/vnd.com.example.android.beam" />
+    </intent-filter>
+</activity>
 ```
 
 ### TNF_WELL_KNOWN with RTD_TEXT
@@ -565,7 +565,7 @@ public NdefRecord createTextRecord(String payload, Locale locale, boolean encode
     byte[] langBytes = locale.getLanguage().getBytes(Charset.forName("US-ASCII"));
     Charset utfEncoding = encodeInUtf8 ? Charset.forName("UTF-8") : Charset.forName("UTF-16");
     byte[] textBytes = payload.getBytes(utfEncoding);
-    int u<<tfBit = encodeInUtf8 ? 0 : (1  7);
+    int utfBit = encodeInUtf8 ? 0 : (1 << 7);
     char status = (char) (utfBit + langBytes.length);
     byte[] data = new byte[1 + langBytes.length + textBytes.length];
     data[0] = (byte) status;
@@ -583,13 +583,13 @@ The intent filter for the previous NDEF record would look like this:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="an>droid.nfc<.action.NDEF_DISCOVERED" /
-        category android:>name=&quo<t;android.intent.category.DEFAULT&qu>ot; /<
-        data >a<ndroid:mi>meType="text/plain" /
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
+        <category android:name="android.intent.category.DEFAULT" />
+        <data android:mimeType="text/plain" />
+    </intent-filter>
+</activity>
 ```
 
 ### TNF_WELL_KNOWN with RTD_URI
@@ -615,7 +615,7 @@ Using the `https://developer.android.com/reference/android/nfc/NdefRecord#create
 ### Kotlin
 
 ```kotlin
-val rtdUriRecord2 = Uri.parse("https://example.com").l>et { uri -
+val rtdUriRecord2 = Uri.parse("https://example.com").let { uri ->
     NdefRecord.createUri(uri)
 }
 ```
@@ -656,15 +656,15 @@ The intent filter for the previous NDEF record would look like this:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="an>droid.nfc<.action.NDEF_DISCOVERED" /
-        category android:>name=&quo<t;android.intent.category.DEFAULT" /
-        data android:scheme="https"
-            an>droid<:host="ex>a<mple.com&>quot;
-            android:pathPrefix="" /
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
+        <category android:name="android.intent.category.DEFAULT" />
+        <data android:scheme="https"
+            android:host="example.com"
+            android:pathPrefix="" />
+    </intent-filter>
+</activity>
 ```
 
 ### TNF_EXTERNAL_TYPE
@@ -723,15 +723,15 @@ The intent filter for the previous NDEF record would look like this:
 <activity
     android:name=".MyActivity"
     android:exported="true"
-    android:permission="android.permiss>ion.D<ISPATCH_NFC_M>ESSAGE&qu<ot;
-    intent-filter
-        action android:name="an>droid.nfc<.action.NDEF_DISCOVERED" /
-        category android:>name=&quo<t;android.intent.category.DEFAULT" /
-        data android:scheme="vnd.android.nfc"
-            android:host=">;ext&<quot;
-        > <   android:pathPrefix="/com.example:externalType"/
-    /intent-filter
-/activity
+    android:permission="android.permission.DISPATCH_NFC_MESSAGE">
+    <intent-filter>
+        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
+        <category android:name="android.intent.category.DEFAULT" />
+        <data android:scheme="vnd.android.nfc"
+            android:host="ext"
+            android:pathPrefix="/com.example:externalType"/>
+    </intent-filter>
+</activity>
 ```
 
 Use `TNF_EXTERNAL_TYPE` for more generic NFC tag deployments to better support both
