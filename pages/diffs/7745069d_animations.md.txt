diff --git a/docs/training/wearables/tiles/animations.md b/docs/training/wearables/tiles/animations.md
index 52d37989..12f2cb59 100644
--- a/docs/training/wearables/tiles/animations.md
+++ b/docs/training/wearables/tiles/animations.md
@@ -12,7 +12,9 @@ Tiles support a few different animation approaches, including the following:
 
 ## Show a sweep transition
 
-To show a smooth sweep from one value to another, you can enable [tween animations](https://wikipedia.org/wiki/Inbetweening) for an element, as shown in the following code snippet:
+To show a smooth sweep from one value to another, you can enable [tween
+animations](https://wikipedia.org/wiki/Inbetweening) for an element, as shown in the following code
+snippet:
 
 ```kotlin
 private var startValue = 15f
@@ -51,12 +53,13 @@ override fun onTileRequest(requestParams: RequestBuilders.TileRequest): Listenab
             .build()
     )
 }
-    
 ```
 
 ### Set arc direction
 
-If your tile contains an arc, you might not want the arc line or text to always grow in the default text direction for the user's chosen language. To specify an arc growth direction, use the `ArcDirection` APIs:
+If your tile contains an arc, you might not want the arc line or text to always
+grow in the default text direction for the user's chosen language. To specify an
+arc growth direction, use the `ArcDirection` APIs:
 
 ```kotlin
 public override fun onTileRequest(
@@ -90,18 +93,25 @@ public override fun onTileRequest(
             .build()
     )
 }
-    
 ```
 
 ## Show a smooth fade or slide
 
-To indicate more clearly that an element is appearing or disappearing in a tile, or to more subtly show a step-change in a tile's value, use fade and slide effects in your tile animations.
+To indicate more clearly that an element is appearing or disappearing in a tile,
+or to more subtly show a step-change in a tile's value, use fade and slide
+effects in your tile animations.
 
-If a tile layout contains an element whose value changes, the tile shows the element's exit animation, then updates the layout and shows the element's enter animation.
+If a tile layout contains an element whose value changes, the tile shows the
+element's exit animation, then updates the layout and shows the element's enter
+animation.
 
 ### Fade transitions
 
-The following code snippet demonstrates how to perform fade-in and fade-out transitions using the helper methods from [`DefaultContentTransitions`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.DefaultContentTransitions). To define custom `FadeInTransition` and `FadeOutTransition` objects, call [`setFadeIn()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.EnterTransition.Builder#setFadeIn(androidx.wear.protolayout.ModifiersBuilders.FadeInTransition)) and [`setFadeOut()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.ExitTransition.Builder#setFadeOut(androidx.wear.protolayout.ModifiersBuilders.FadeOutTransition)), respectively, in the transition setter methods.
+The following code snippet demonstrates how to perform fade-in and fade-out
+transitions using the helper methods from [`DefaultContentTransitions`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.DefaultContentTransitions). To
+define custom `FadeInTransition` and `FadeOutTransition` objects, call
+[`setFadeIn()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.EnterTransition.Builder#setFadeIn(androidx.wear.protolayout.ModifiersBuilders.FadeInTransition)) and [`setFadeOut()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.ExitTransition.Builder#setFadeOut(androidx.wear.protolayout.ModifiersBuilders.FadeOutTransition)), respectively, in the transition
+setter methods.
 
 ```kotlin
 public override fun onTileRequest(
@@ -132,12 +142,15 @@ public override fun onTileRequest(
             .build()
     )
 }
-    
 ```
 
 ### Slide transitions
 
-This other code snippet demonstrates how to perform slide-in and slide-out transitions using the helper methods from [`DefaultContentTransitions`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.DefaultContentTransitions). You can also define custom `SlideInTransition` and `SlideOutTransition` objects by calling [`setSlideIn()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.EnterTransition.Builder#setSlideIn(androidx.wear.protolayout.ModifiersBuilders.SlideInTransition)) and [`setSlideOut()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.ExitTransition.Builder#setSlideOut(androidx.wear.protolayout.ModifiersBuilders.SlideOutTransition)), respectively, in the transition setter methods.
+This other code snippet demonstrates how to perform slide-in and slide-out
+transitions using the helper methods from [`DefaultContentTransitions`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.DefaultContentTransitions). You
+can also define custom `SlideInTransition` and `SlideOutTransition` objects by
+calling [`setSlideIn()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.EnterTransition.Builder#setSlideIn(androidx.wear.protolayout.ModifiersBuilders.SlideInTransition)) and [`setSlideOut()`](https://developer.android.com/reference/androidx/wear/protolayout/ModifiersBuilders.ExitTransition.Builder#setSlideOut(androidx.wear.protolayout.ModifiersBuilders.SlideOutTransition)), respectively, in the
+transition setter methods.
 
 ```kotlin
 public override fun onTileRequest(
@@ -176,18 +189,20 @@ public override fun onTileRequest(
             .build()
     )
 }
-    
 ```
 
 ## Show a transformation
 
-To call attention to a specific element or area in a tile, you can apply several types of transformations to it, including: rotation, scaling, and translation.
+To call attention to a specific element or area in a tile, you can apply several
+types of transformations to it, including: rotation, scaling, and translation.
 
-Many floating-point values associated with transformations accept [dynamic expressions](https://developer.android.com/training/wearables/tiles/dynamic#associate-with-data-sources), which let you animate these transformations.
+Many floating-point values associated with transformations accept [dynamic
+expressions](https://developer.android.com/training/wearables/tiles/dynamic#associate-with-data-sources), which let you animate these transformations.
 
 ### Rotation
 
-To perform a clockwise rotation about a customizable pivot point, use code similar to the following:
+To perform a clockwise rotation about a customizable pivot point, use code
+similar to the following:
 
 ```kotlin
 return Futures.immediateFuture(
@@ -215,12 +230,12 @@ return Futures.immediateFuture(
         )
         .build()
 )
-    
 ```
 
 ### Scaling
 
-To grow or shrink an element by horizontal and vertical scaling factors, use code similar to the following:
+To grow or shrink an element by horizontal and vertical scaling factors, use
+code similar to the following:
 
 ```kotlin
 return Futures.immediateFuture(
@@ -250,12 +265,12 @@ return Futures.immediateFuture(
         )
         .build()
 )
-    
 ```
 
 ### Geometric translation
 
-To move an element by a specific number of density pixels (dp) across the screen horizontally or vertically, use code similar to the following:
+To move an element by a specific number of density pixels (dp) across the screen
+horizontally or vertically, use code similar to the following:
 
 ```kotlin
 return Futures.immediateFuture(
@@ -281,12 +296,12 @@ return Futures.immediateFuture(
         )
         .build()
 )
-    
 ```
 
 ## Lottie animations
 
-Tiles supports the playback of [Lottie animations](https://lottie.github.io/), using a syntax similar to that of images:
+Tiles supports the playback of [Lottie animations](https://lottie.github.io/), using a syntax similar
+to that of images:
 
 ```kotlin
 class LottieAnimation : TileService() {
@@ -331,8 +346,6 @@ class LottieAnimation : TileService() {
         )
     }
 }
-
-    
 ```
 
 A few points to note:
@@ -349,4 +362,6 @@ There are several situations in which animations are disabled:
 - The system's tile render might disable animations for all tiles.
 - A tile can animate only 4 elements at a time. If you try to animate more than 4 elements at the same time, not all of them show an animation.
 
-In the case where an animation is disabled, the elements are static and show the animation's end value. For this reason, don't rely on the animation's behavior, such as its duration, to show important information.
\ No newline at end of file
+In the case where an animation is disabled, the elements are static and show the
+animation's end value. For this reason, don't rely on the animation's behavior,
+such as its duration, to show important information.
\ No newline at end of file
