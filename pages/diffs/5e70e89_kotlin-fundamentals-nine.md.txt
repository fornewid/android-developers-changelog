diff --git a/docs/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine.md b/docs/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine.md
index b125d30..c72a6ac 100644
--- a/docs/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine.md
+++ b/docs/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine.md
@@ -1,88 +1,74 @@
 ---
-title: Kotlin Fundamentals: Repository and WorkManager Quiz  |  Android Developers
+title: https://developer.android.com/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine
 url: https://developer.android.com/courses/quizzes/kotlin-fundamentals-nine/kotlin-fundamentals-nine
-source: html-scrape
+source: md.txt
 ---
 
-Stay organized with collections
-
-Save and categorize content based on your preferences.
-
-
-
 # Kotlin Fundamentals: Repository and WorkManager Quiz
 
-Return to pathway
-
-
-
-1. ## Which component in the Android Architecture Components is responsible for keeping the offline cache up-to-date and getting data from the network?
-
-   ViewModel
-
-   LiveData
-
-   Repository
-
-   Activities
-2. ## What is the best way to save structured data on the device file system for offline caching?
-
-   Room
-
-   Files
-
-   Shared preferences
-
-   Retrofit caching
-3. ## The Transformations.map() converts one LiveData into another \_\_\_.
-
-   ViewModel
-
-   LiveData
-
-   Repository
-
-   DAO object
-4. ## When implementing offline caching, which one of the following strategies is a good example of the separation of concerns principle?
-
-   Create separate classes to represent the network, domain, and database objects.
+# Kotlin Fundamentals: Repository and WorkManager Quiz
 
-   Create a single class to represent the network, domain, and database objects.
+<br />
 
-   Create a single class to represent the network and domain objects, and another class to represent the database object.
+Return to pathway  
+1.
 
-   Create a single class to represent the network object and another class to represent the database and domain objects.
-5. ## Which of the following are concrete implementations of the WorkRequest class? Select all that apply.
+   ## Which component in the Android Architecture Components is responsible for keeping the offline cache up-to-date and getting data from the network?
 
-   Choose as many answers as you see fit.
+   ViewModel  
+   LiveData  
+   Repository  
+   Activities  
+2.
 
-   ListenableWorker class is for runtime tasks.
+   ## What is the best way to save structured data on the device file system for offline caching?
 
-   OneTimeWorkRequest class is for one-off tasks.
+   Room  
+   Files  
+   Shared preferences  
+   Retrofit caching  
+3.
 
-   PeriodicWorkRequest class is for periodic work, work that repeats at intervals.
+   ## The Transformations.map()converts one LiveData into another ___.
 
-   RxWorker class is an interoperability Worker implementation for one-off tasks.
-6. ## Which of the following classes does the WorkManager use to schedule the background task on API 23 and higher?
+   ViewModel  
+   LiveData  
+   Repository  
+   DAO object  
+4.
 
-   Only JobScheduler
+   ## When implementing offline caching, which one of the following strategies is a good example of the separation of concerns principle?
 
-   BroadcastReceiver and AlarmManager
+   Create separate classes to represent the network, domain, and database objects.  
+   Create a single class to represent the network, domain, and database objects.  
+   Create a single class to represent the network and domain objects, and another class to represent the database object.  
+   Create a single class to represent the network object and another class to represent the database and domain objects.  
+5.
 
-   AlarmManager and JobScheduler
+   ## Which of the following are concrete implementations of the WorkRequest class? Select all that apply.
 
-   Scheduler and BroadcastReceiver
-7. ## Which API do you use to add constraints to a WorkRequest?
+   Choose as many answers as you see fit.  
+   ListenableWorker class is for runtime tasks.  
+   OneTimeWorkRequest class is for one-off tasks.  
+   PeriodicWorkRequest class is for periodic work, work that repeats at intervals.  
+   RxWorker class is an interoperability Worker implementation for one-off tasks.  
+6.
 
-   setConstraints()
+   ## Which of the following classes does the WorkManager use to schedule the background task on API 23 and higher?
 
-   addConstraints()
+   Only JobScheduler  
+   BroadcastReceiver and AlarmManager  
+   AlarmManager and JobScheduler  
+   Scheduler and BroadcastReceiver  
+7.
 
-   setRequiresCharging(true)
+   ## Which API do you use to add constraints to a WorkRequest?
 
-   addConstraintsToWorkRequest(true)
+   setConstraints()  
+   addConstraints()  
+   setRequiresCharging(true)  
+   addConstraintsToWorkRequest(true)  
 
 Submit answers
 
-*error\_outline*
-An error occurred when grading the quiz. Please try again.
\ No newline at end of file
+*error_outline*An error occurred when grading the quiz. Please try again.
\ No newline at end of file
