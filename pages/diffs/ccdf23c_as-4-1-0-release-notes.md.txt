diff --git a/docs/studio/releases/past-releases/as-4-1-0-release-notes.md b/docs/studio/releases/past-releases/as-4-1-0-release-notes.md
index f6b713a..3f99be8 100644
--- a/docs/studio/releases/past-releases/as-4-1-0-release-notes.md
+++ b/docs/studio/releases/past-releases/as-4-1-0-release-notes.md
@@ -15,7 +15,7 @@ and improvements.
 
 <br />
 
-**4.1.3 (March 2021)**
+[Video](https://www.youtube.com/watch?v=Yhbr6u7f3ME) **4.1.3 (March 2021)**
 
 
 This minor update includes various bug fixes.
@@ -296,10 +296,11 @@ For basic usage instructions on how to record a system trace, see the
 
 <br />
 
-| **Windows users:** If labels are missing in the thread
-| activity timeline when you profile your app, see the
-| [known issues](https://developer.android.com/studio/known-issues#system-trace-labels)
-| for a workaround. This issue is fixed in the 4.2 release.
+> [!NOTE]
+> **Windows users:** If labels are missing in the thread
+> activity timeline when you profile your app, see the
+> [known issues](https://developer.android.com/studio/known-issues#system-trace-labels)
+> for a workaround. This issue is fixed in the 4.2 release.
 
 <br />
 
@@ -382,7 +383,7 @@ guidance for themes and styles by default. Updates include:
   `colorPrimary`).
 
 - **Theme resources** : Theme resources are in `themes.xml`
-  (instead of `styles.xml`) and use `Theme.<var>`<var> names.
+  (instead of `styles.xml`) and use `Theme.<var><var>` names.
 
 - **Dark theme** : Base application themes use `DayNight`
   parents and are split between `res/values` and `res/values-night`.
