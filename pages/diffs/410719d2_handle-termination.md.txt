diff --git a/docs/develop/ui/views/layout/webapps/handle-termination.md b/docs/develop/ui/views/layout/webapps/handle-termination.md
index 6c4fc82b..7b356161 100644
--- a/docs/develop/ui/views/layout/webapps/handle-termination.md
+++ b/docs/develop/ui/views/layout/webapps/handle-termination.md
@@ -146,4 +146,5 @@ When implementing `onRenderProcessGone`, keep the following considerations in
 mind to ensure app stability and maintain visibility into termination events:
 
 - **Repeated crashes:** If a renderer crashes while loading a specific page, don't just reload the same page immediately. It might cause the new WebView to crash again.
-- **Visibility:** App kills caused by missing this callback aren't yet reflected in Play Console crash reports. While future updates might address this gap, you must resolve these issues now to ensure app stability. Implementing this handler provides immediate visibility into why your app exits.
\ No newline at end of file
+- **Visibility:** App kills caused by missing this callback aren't yet reflected in Play Console crash reports. While future updates might address this gap, you must resolve these issues now to ensure app stability. Implementing this handler provides immediate visibility into why your app exits.
+- **Distinguish renderer termination from app process death:** `onRenderProcessGone` handles situations where the renderer process is terminated while your app process remains active. If the entire app process is killed by the system in the background, use `onSaveInstanceState()` with [`WebView.saveState()`](https://developer.android.com/reference/android/webkit/WebView#saveState(android.os.Bundle)) and [`WebView.restoreState()`](https://developer.android.com/reference/android/webkit/WebView#restoreState(android.os.Bundle)) to persist and recover navigation history. To manage saved state safely without hitting transaction limits, see [Manage WebView state efficiently](https://developer.android.com/develop/ui/views/layout/webapps/manage-state).
\ No newline at end of file
