diff --git a/docs/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2.md b/docs/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2.md
index ad4b7f3..5eacf79 100644
--- a/docs/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2.md
+++ b/docs/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2.md
@@ -1,145 +1,107 @@
 ---
-title: Getting Data From the Internet  |  Android Developers
+title: https://developer.android.com/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2
 url: https://developer.android.com/courses/quizzes/android-basics-kotlin-unit-4-pathway-2/android-basics-kotlin-unit-4-pathway-2
-source: html-scrape
+source: md.txt
 ---
 
-Stay organized with collections
-
-Save and categorize content based on your preferences.
-
-
-
 # Getting Data From the Internet
 
-Return to pathway
-
-
-
-1. ## Which of the following are required to load and display an image with coil?
-
-   Choose as many answers as you see fit.
-
-   A placeholder image
-
-   An error image
-
-   The URL of the image
-
-   An `ImageView`
-2. ## Match the element of HTTP requests and responses to the correct term.
-
-   Each answer only matches one item.
-
-   Status Message
-
-   POST
-   200
-   OK
-   application/json
-
-   Status Code
-
-   POST
-   200
-   OK
-   application/json
-
-   HTTP Verb
-
-   POST
-   200
-   OK
-   application/json
-
-   Content-Type
-
-   POST
-   200
-   OK
-   application/json
-3. ## Which of the following are characteristics of a RESTful service?
-
-   Choose as many answers as you see fit.
-
-   Stateless
-
-   JSON responses
-
-   Client-server-architecture
-
-   Resources exposed as URIs
-4. ## Which of the following are needed to make a network request with Retrofit?
-
-   Choose as many answers as you see fit.
-
-   Add the `android.permission.INTERNET` permission to the manifest
-
-   Include Retrofit Gradle dependency
-
-   Supply a base URL to the `Retrofit.Builder` object,
-
-   Add coil library as a Gradle dependency.
-5. ## Why do you declare `DataProviderManager` with the `object` keyword?
-
-   Choose as many answers as you see fit.
-
-   To make this object a singleton
-
-   A shared object provides a convenient way to store state
-
-   You only need one instance of the object.
+# Getting Data From the Internet
 
-   `DataProviderManager` is an object and not a class.
-6. ## Which of the following are true about grid layouts?
+<br />
 
-   Choose as many answers as you see fit.
+Return to pathway  
+1.
 
-   By default, a grid layout scrolls vertically
+   ## Which of the following are required to load and display an image with coil?
 
-   A grid layout is ideal for lists that can be represented as icons or images
+   Choose as many answers as you see fit.  
+   A placeholder image  
+   An error image  
+   The URL of the image  
+   An`ImageView`  
+2.
 
-   A span is equal to the width of one row
+   ## Match the element of HTTP requests and responses to the correct term.
 
-   You can reference the object by its type name, `DataProviderManager`.
-7. ## In the URL https://google.com/search?q=android, /search is called the \_\_\_.
+   Each answer only matches one item.  
+   Status Message  
+   POST200OKapplication/json  
+   Status Code  
+   POST200OKapplication/json  
+   HTTP Verb  
+   POST200OKapplication/json  
+   Content-Type  
+   POST200OKapplication/json  
+3.
 
-   Host
+   ## Which of the following are characteristics of a RESTful service?
 
-   Path
+   Choose as many answers as you see fit.  
+   Stateless  
+   JSON responses  
+   Client-server-architecture  
+   Resources exposed as URIs  
+4.
 
-   Query
+   ## Which of the following are needed to make a network request with Retrofit?
 
-   Parameters
-8. ## Which of the following are true about binding adapters?
+   Choose as many answers as you see fit.  
+   Add the`android.permission.INTERNET`permission to the manifest  
+   Include Retrofit Gradle dependency  
+   Supply a base URL to the`Retrofit.Builder`object,  
+   Add coil library as a Gradle dependency.  
+5.
 
-   Choose as many answers as you see fit.
+   ## Why do you declare`DataProviderManager`with the`object`keyword?
 
-   A binding adapter only provides an alternate way to set a hardcoded value.
+   Choose as many answers as you see fit.  
+   To make this object a singleton  
+   A shared object provides a convenient way to store state  
+   You only need one instance of the object.  
+   `DataProviderManager`is an object and not a class.  
+6.
 
-   Binding adapters allow you to implement custom logic for how layout attributes are assigned.
+   ## Which of the following are true about grid layouts?
 
-   The `@BindingAdapter` annotation requires the view as an argument
+   Choose as many answers as you see fit.  
+   By default, a grid layout scrolls vertically  
+   A grid layout is ideal for lists that can be represented as icons or images  
+   A span is equal to the width of one row  
+   You can reference the object by its type name,`DataProviderManager`.  
+7.
 
-   `@BindingAdapter` methods process custom values that are provided to XML attributes.
-9. ## True or False: Retrofit creates the code required to make network requests based on a web service.
+   ## In the URL https://google.com/search?q=android, /search is called the ___.
 
-   True
+   Host  
+   Path  
+   Query  
+   Parameters  
+8.
 
-   False
-10. ## The Moshi library requires the following to successfully process JSON in Kotlin.
+   ## Which of the following are true about binding adapters?
 
-    Choose as many answers as you see fit.
+   Choose as many answers as you see fit.  
+   A binding adapter only provides an alternate way to set a hardcoded value.  
+   Binding adapters allow you to implement custom logic for how layout attributes are assigned.  
+   The`@BindingAdapter`annotation requires the view as an argument  
+   `@BindingAdapter`methods process custom values that are provided to XML attributes.  
+9.
 
-    A data class describing the structure of the JSON object.
+   ## True or False: Retrofit creates the code required to make network requests based on a web service.
 
-    `KotlinJsonAdapterFactory()` added to the `Moshi.Builder` object.
+   True  
+   False  
+10.
 
-    A `MoshiConverterFactory` added to the `Retrofit.Builder` object.
+    ## The Moshi library requires the following to successfully process JSON in Kotlin.
 
-    All properties in the data class marked with the `@JSON` annotation.
+    Choose as many answers as you see fit.  
+    A data class describing the structure of the JSON object.  
+    `KotlinJsonAdapterFactory()`added to the`Moshi.Builder`object.  
+    A`MoshiConverterFactory`added to the`Retrofit.Builder`object.  
+    All properties in the data class marked with the`@JSON`annotation.  
 
 Submit answers
 
-*error\_outline*
-An error occurred when grading the quiz. Please try again.
\ No newline at end of file
+*error_outline*An error occurred when grading the quiz. Please try again.
\ No newline at end of file
