diff --git a/docs/agents/skills/build/agp/agp-9-upgrade/references/recipes.md b/docs/agents/skills/build/agp/agp-9-upgrade/references/recipes.md
index 125d970..3b856ca 100644
--- a/docs/agents/skills/build/agp/agp-9-upgrade/references/recipes.md
+++ b/docs/agents/skills/build/agp/agp-9-upgrade/references/recipes.md
@@ -1,15 +1,19 @@
 ---
-title: https://developer.android.com/agents/skills/build/agp/agp-9-upgrade/references/recipes
+title: Gradle recipes  |  Android Developers
 url: https://developer.android.com/agents/skills/build/agp/agp-9-upgrade/references/recipes
-source: md.txt
+source: html-scrape
 ---
 
+# Gradle recipes Stay organized with collections Save and categorize content based on your preferences.
+
+
+
 When migrating to AGP's new DSL, any Gradle code (plugins or logic in build
 scripts) that relied on the old DSL will stop working. Such code must be
 migrated.
 
 In some cases, there is a one-to-one replacement for the old code. Some examples
-are in [the AGP 9.0.0 release notes](https://developer.android.com/build/releases/agp-9-0-0-release-notes).
+are in [the AGP 9.0.0 release notes](/build/releases/agp-9-0-0-release-notes).
 
 In other cases, there is no direct one-to-one replacement. For these situations,
 the [gradle-recipes repo](https://github.com/android/gradle-recipes) is a great resource. You can checkout one of its
@@ -20,7 +24,7 @@ lists the compatibility for recipes for each version of AGP.
 ## Compatibility table
 
 | AGP version | gradle-recipes branch |
-|---|---|
+| --- | --- |
 | 9.0.x | agp-9.0 |
 | 9.1.x | agp-9.1 |
 | 9.2.x | agp-9.2 |
@@ -30,7 +34,7 @@ lists the compatibility for recipes for each version of AGP.
 The following table links use-cases to recipes.
 
 | Recipe | Use-case |
-|---|---|
+| --- | --- |
 | addCustomBuildConfigFields | Add custom BuildConfig fields |
 | listenToArtifacts | Rename APK |
 
@@ -38,23 +42,25 @@ Additional details for each use-case follow.
 
 ### Add custom BuildConfig fields
 
-See the detailed guide at [BuildConfig](https://developer.android.com/agents/skills/build/agp/agp-9-upgrade/references/buildconfig).
+See the detailed guide at [BuildConfig](/agents/skills/build/agp/agp-9-upgrade/references/buildconfig).
 
 ### Renaming an APK
 
 In the old DSL, an APK could be renamed very simply. Here's an example:
 
-    android {
-      applicationVariants.all {
-        outputs.all {
-          val output = this as com.android.build.gradle.api.ApkVariantOutput
-          val fileName = output.outputFileName
-          if (fileName.contains("release")) {
-            output.outputFileName = "my-cool-new-name.apk"
-          }
-        }
+```
+android {
+  applicationVariants.all {
+    outputs.all {
+      val output = this as com.android.build.gradle.api.ApkVariantOutput
+      val fileName = output.outputFileName
+      if (fileName.contains("release")) {
+        output.outputFileName = "my-cool-new-name.apk"
       }
     }
+  }
+}
+```
 
 However, with AGP 9 and the new DSL, `applicationVariants` is no longer
 available. You must instead react to artifact creation using the
