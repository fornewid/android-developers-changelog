diff --git a/docs/develop/ui/compose/styles/fundamentals.md b/docs/develop/ui/compose/styles/fundamentals.md
index 102ea779..92abdb56 100644
--- a/docs/develop/ui/compose/styles/fundamentals.md
+++ b/docs/develop/ui/compose/styles/fundamentals.md
@@ -48,7 +48,8 @@ BaseButton(
     onClick = { },
     style = { }
 ) {
-    BaseText("Click mnippets.kt
+    BaseText("Click me")
+}
 ```
 
 <br />
@@ -62,7 +63,8 @@ BaseButton(
     onClick = { },
     style = { background(Color.Blue) }
 ) {
-    BaseText("Click mnippets.kt
+    BaseText("Click me")
+}
 ```
 
 <br />
@@ -81,7 +83,8 @@ own custom components.
 Row(
     modifier = Modifier.styleable { }
 ) {
-    BaseText("Contennippets.kt
+    BaseText("Content")
+}
 ```
 
 <br />
@@ -96,7 +99,8 @@ Row(
         background(Color.Blue)
     }
 ) {
-    BaseText("Contennippets.kt
+    BaseText("Content")
+}
 ```
 
 <br />
@@ -147,8 +151,8 @@ val styleState = remember { MutableStyleState(null) }
 Column(
     Modifier.styleable(styleState, style)
 ) {
-    BaseText("Col)
-}StylesSnippets.kt
+    BaseText("Column content")
+}
 ```
 
 <br />
@@ -174,8 +178,10 @@ Column(
 }
 val rowStyleState = remember { MutableStyleState(null) }
 Row(
-    Modifier.styleable(rowStyleState, stText("Row")
-}StylesSnippets.kt
+    Modifier.styleable(rowStyleState, style)
+) {
+    BaseText("Row")
+}
 ```
 
 <br />
@@ -194,7 +200,8 @@ BaseButton(
         contentPaddingStart(16.dp)
     }
 ) {
-    BaseText("Buttonippets.kt
+    BaseText("Button")
+}
 ```
 
 <br />
@@ -224,7 +231,8 @@ BaseButton(
         //
     }
 ) {
-    BaseText("Click menippets.kt
+    BaseText("Click me!")
+}
 ```
 
 <br />
@@ -248,7 +256,8 @@ BaseButton(
 
     },
 ) {
-    BaseText("Click menippets.kt
+    BaseText("Click me!")
+}
 ```
 
 <br />
@@ -280,7 +289,8 @@ BaseButton(
 
     },
 ) {
-    BaseText("Click menippets.kt
+    BaseText("Click me!")
+}
 ```
 
 <br />
@@ -330,8 +340,8 @@ Column(
 ) {
     BaseText("Children inherit", style = { width(60.dp) })
     BaseText("certain properties")
-    BaseText(&quents")
-}StylesSnippets.kt
+    BaseText("from their parents")
+}
 ```
 
 <br />
@@ -361,8 +371,8 @@ Column(
         contentBrush(Brush.linearGradient(listOf(Color.Red, Color.Blue)))
     })
     BaseText("override properties")
-    BaseText("ents")
-}StylesSnippets.kt
+    BaseText("set by their parents")
+}
 ```
 
 <br />
