diff --git a/docs/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one.md b/docs/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one.md
index 5dff9b4..1ec958a 100644
--- a/docs/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one.md
+++ b/docs/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one.md
@@ -1,64 +1,73 @@
 ---
-title: https://developer.android.com/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one
+title: Introduction to Kotlin  |  Android Developers
 url: https://developer.android.com/courses/quizzes/android-basics-kotlin-one/android-basics-kotlin-pathway-one
-source: md.txt
+source: html-scrape
 ---
 
-# Introduction to Kotlin
+Stay organized with collections
+
+Save and categorize content based on your preferences.
+
+
 
 # Introduction to Kotlin
 
-<br />
+Return to pathway
+
+
+
+1. ## What is a program?
 
+   A specific task
 
-Return to pathway  
-1.
+   A tool that helps you write Kotlin code
 
-   ## What is a program?
+   A series of instructions that a computer system executes to accomplish some action
 
-   A specific task  
-   A tool that helps you write Kotlin code  
-   A series of instructions that a computer system executes to accomplish some action  
-   A defined set of instructions that tells your computer to print "Happy Birthday!"  
-2.
+   A defined set of instructions that tells your computer to print “Happy Birthday!”
+2. ## Which keyword do you use to define a function in Kotlin?
 
-   ## Which keyword do you use to define a function in Kotlin?
+   `fun`
 
-   `fun`  
-   `func`  
-   `function`  
-   `newFunction`  
-   `main`  
-3.
+   `func`
 
-   ## Which of the following do you need to create a Kotlin program that prints a line of text?
+   `function`
 
+   `newFunction`
 
-   Choose as many answers as you see fit.  
-   comment describing what your program does  
-   a `main()` function  
-   curly braces `{}` around the instructions to the system  
-   a call to `print()` or `println()`  
-   a repeat() statement  
-4.
+   `main`
+3. ## Which of the following do you need to create a Kotlin program that prints a line of text?
 
-   ## What do you expect this Kotlin code to do?
+   Choose as many answers as you see fit.
 
-   ```kotlin
+   comment describing what your program does
+
+   a `main()` function
+
+   curly braces `{}` around the instructions to the system
+
+   a call to `print()` or `println()`
+
+   a repeat() statement
+4. ## What do you expect this Kotlin code to do?
+
+   ```
    fun main(args: Array&ltString&gt) {
      println("Hello, world!")
      println("It's a sunny and warm day!")
    }
    ```
-   Print one line of text  
-   Print two lines of text  
-   Print three lines of text  
-   Print two lines of text separated by a blank line  
-5.
 
-   ## How would you modify this `main()` function so that it prints a 6-layer cake for someone's 4th birthday?
+   Print one line of text
 
-   ```kotlin
+   Print two lines of text
+
+   Print three lines of text
+
+   Print two lines of text separated by a blank line
+5. ## How would you modify this `main()` function so that it prints a 6-layer cake for someone's 4th birthday?
+
+   ```
    fun main() {
      val age = 24
      val layers = 5
@@ -67,25 +76,31 @@ Return to pathway
      printCakeBottom(age, layers)
    }
    ```
-   Set val age to `6`, set val layers to `4`  
-   Set val age to `"4"`, set val layers to `"6"`  
-   Set val age to `4`, set val layers to `6`  
-   Leave the code as-is  
-6.
 
-   ## Which of these options correctly calls the function, below, and passes it valid input arguments?
+   Set val age to `6`, set val layers to `4`
+
+   Set val age to `"4"`, set val layers to `"6"`
+
+   Set val age to `4`, set val layers to `6`
+
+   Leave the code as-is
+6. ## Which of these options correctly calls the function, below, and passes it valid input arguments?
 
-   ```kotlin
+   ```
    fun createMessage(name: String, location: String, age: Int) {
      println("My name is ${name}. I am from ${location}, and I am ${age} years old.")
    }
    ```
-   `createMessage("Amy", "Australia", 20)`  
-   `createMessage("Evan", England, 9)`  
-   `createmessage("Tom", "Thailand", "40")`  
-   `createMessage(Heather, "Haiti", 7)`  
 
-Submit answers
+   `createMessage("Amy", "Australia", 20)`
+
+   `createMessage("Evan", England, 9)`
 
+   `createmessage("Tom", "Thailand", “40”)`
+
+   `createMessage(Heather, “Haiti”, 7)`
+
+Submit answers
 
-*error_outline* An error occurred when grading the quiz. Please try again.
\ No newline at end of file
+*error\_outline*
+An error occurred when grading the quiz. Please try again.
\ No newline at end of file
