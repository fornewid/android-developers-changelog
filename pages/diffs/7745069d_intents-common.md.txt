diff --git a/docs/guide/components/intents-common.md b/docs/guide/components/intents-common.md
index b9578faa..b788514d 100644
--- a/docs/guide/components/intents-common.md
+++ b/docs/guide/components/intents-common.md
@@ -4,95 +4,112 @@ url: https://developer.android.com/guide/components/intents-common
 source: md.txt
 ---
 
-An intent lets you start an activity in another app by describing an action you'd like to perform, such as "view a map" or "take a picture," in an `https://developer.android.com/reference/android/content/Intent` object. This type of intent is called an *implicit* intent because it doesn't specify the app component to start, but instead specifies an *action* and provides some *data* with which to perform the action.
+An intent lets you start an activity in another app by describing an
+action you'd like to perform, such as "view a map" or "take a
+picture," in an `https://developer.android.com/reference/android/content/Intent` object. This type of intent
+is called an *implicit* intent because it doesn't specify the app
+component to start, but instead specifies an *action* and provides
+some *data* with which to perform the action.
 
-When you call `https://developer.android.com/reference/android/content/Context#startActivity(android.content.Intent)` or `https://developer.android.com/reference/android/app/Activity#startActivityForResult(android.content.Intent, int)` and pass it an implicit intent, the system [resolves the intent](https://developer.android.com/guide/components/intents-filters#Resolution) to an app that can handle the intent and starts its corresponding `https://developer.android.com/reference/android/app/Activity`. If there's more than one app that can handle the intent, the system presents the user with a dialog to pick which app to use.
 
-This page describes several implicit intents that you can use to perform common actions, organized by the type of app that handles the intent. Each section also shows how you can create an [intent filter](https://developer.android.com/guide/components/intents-filters#Receiving) to advertise your app's ability to perform the action.
+When you call `https://developer.android.com/reference/android/content/Context#startActivity(android.content.Intent)`
+or `https://developer.android.com/reference/android/app/Activity#startActivityForResult(android.content.Intent, int)` and pass it an implicit intent, the system
+[resolves
+the intent](https://developer.android.com/guide/components/intents-filters#Resolution) to an app that can handle the intent and starts its
+corresponding `https://developer.android.com/reference/android/app/Activity`. If there's more than one app
+that can handle the intent, the system presents the user with a dialog to
+pick which app to use.
 
-**Caution:** If there are no apps on the device that can receive an implicit intent, an app crashes when it calls `https://developer.android.com/reference/android/content/Context#startActivity(android.content.Intent)`. To first verify that an app exists to receive the intent, call `https://developer.android.com/reference/android/content/Intent#resolveActivity(android.content.pm.PackageManager)` on your `https://developer.android.com/reference/android/content/Intent` object. If the result is non-null, there is at least one app that can handle the intent, and it's safe to call `startActivity()`. If the result is null, don't use the intent and, if possible, disable the feature that invokes the intent.
 
-If you're not familiar with how to create intents or intent filters, first read [Intents and Intent Filters](https://developer.android.com/guide/components/intents-filters).
+This page describes several implicit intents that you can use to perform
+common actions, organized by the type of app that handles the intent. Each
+section also shows how you can create an [intent
+filter](https://developer.android.com/guide/components/intents-filters#Receiving) to advertise your app's ability to perform the action.
 
-To learn how to fire the intents listed on this page from your development host, see the [Verify intents with the Android Debug Bridge](https://developer.android.com/guide/components/intents-common#AdbIntents) section.
+
+**Caution:** If there are no apps on the device that can
+receive an implicit intent, an app crashes when it calls `https://developer.android.com/reference/android/content/Context#startActivity(android.content.Intent)`. To first verify that
+an app exists to receive the intent, call `https://developer.android.com/reference/android/content/Intent#resolveActivity(android.content.pm.PackageManager)` on your `https://developer.android.com/reference/android/content/Intent` object. If the result is non-null, there is at least
+one app that can handle the intent, and it's safe to call `startActivity()`. If the result is
+null, don't use the intent and, if possible, disable the
+feature that invokes the intent.
+
+
+If you're not familiar with how to create intents or intent filters, first read [Intents and Intent
+Filters](https://developer.android.com/guide/components/intents-filters).
+
+
+To learn how to fire the intents listed on this page from your development
+host, see the [Verify intents with the Android Debug
+Bridge](https://developer.android.com/guide/components/intents-common#AdbIntents) section.
 
 #### Google Voice Actions
 
-[Google Voice Actions](https://developers.google.com/voice-actions/) fires some of the intents listed on this page in response to voice commands. For more information, see [Get Started with System Voice Actions](https://developers.google.com/voice-actions/system/#system_actions_reference).
+
+[Google Voice
+Actions](https://developers.google.com/voice-actions/) fires some of the intents listed on this page in response to
+voice commands. For more information, see [Get Started with System Voice Actions](https://developers.google.com/voice-actions/system/#system_actions_reference).
 
 ## Alarm clock
 
-The following are common actions for alarm clock apps, including the information you need to create an intent filter to advertise your app's ability to perform each action.
+The following are common actions for alarm clock apps, including the information you need
+to create an intent filter to advertise your app's ability to perform each action.
 
 ### Create an alarm
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "set an alarm for 7 am"
 
-To create a new alarm, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM` action and specify alarm details such as the time and message using the following extras.
-
-**Note:** Only the hour, minutes, and message extras are available in Android 2.3 (API level 9) and lower. The other extras are available in higher versions of the platform.
+To create a new alarm, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM`
+action and specify alarm details such as the time and message using the following extras.
 
+**Note:** Only the hour, minutes, and message extras are available
+in Android 2.3 (API level 9) and lower. The other extras are available in higher versions of the
+platform.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM`
-
+:   `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM`
 
 **Data URI**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_HOUR`
-    :
-        The hour for the alarm.
-
+    :   The hour for the alarm.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_MINUTES`
-    :
-        The minutes for the alarm.
-
+    :   The minutes for the alarm.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_MESSAGE`
-    :
-        A custom message to identify the alarm.
-
+    :   A custom message to identify the alarm.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_DAYS`
-    :
-        An `https://developer.android.com/reference/java/util/ArrayList` including each week day on which this alarm repeats. Each day must be declared with an integer from the `https://developer.android.com/reference/java/util/Calendar` class, such as `https://developer.android.com/reference/java/util/Calendar#MONDAY`.
+    :   An `https://developer.android.com/reference/java/util/ArrayList` including each week day on which this alarm
+        repeats. Each day must be declared with an integer from the `https://developer.android.com/reference/java/util/Calendar`
+        class, such as `https://developer.android.com/reference/java/util/Calendar#MONDAY`.
 
         For a one-time alarm, don't specify this extra.
 
-
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_RINGTONE`
-    :
-        A `content:` URI specifying a ringtone to use with the alarm, or `https://developer.android.com/reference/android/provider/AlarmClock#VALUE_RINGTONE_SILENT` for no ringtone.
+    :   A `content:` URI specifying a ringtone to use with the alarm, or `https://developer.android.com/reference/android/provider/AlarmClock#VALUE_RINGTONE_SILENT` for no ringtone.
 
         To use the default ringtone, don't specify this extra.
 
-
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_VIBRATE`
-    :
-        A boolean specifying whether to vibrate for this alarm.
-
+    :   A boolean specifying whether to vibrate for this alarm.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_SKIP_UI`
-    :
-        A boolean specifying whether the responding app must skip its UI when setting the alarm. If true, the app must bypass any confirmation UI and set the specified alarm.
+    :   A boolean specifying whether the responding app must skip its UI when setting the alarm.
+        If true, the app must bypass any confirmation UI and set the specified alarm.
 
 **Example intent:**
 
@@ -126,7 +143,8 @@ public void createAlarm(String message, int hour, int minutes) {
 ```
 **Note:**
 
-To invoke the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM` intent, your app must have the `https://developer.android.com/reference/android/Manifest.permission#SET_ALARM` permission:
+To invoke the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_ALARM` intent, your app must have the
+`https://developer.android.com/reference/android/Manifest.permission#SET_ALARM` permission:
 
 ```xml
 <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
@@ -147,47 +165,39 @@ To invoke the `https://developer.android.com/reference/android/provider/AlarmClo
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "set timer for 5 minutes"
 
-To create a countdown timer, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER` action and specify timer details such as the duration using the following extras.
 
-**Note:** This intent is available in Android 4.4 (API level 19) and higher.
+To create a countdown timer, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER` action and specify timer
+details such as the duration using the following extras.
 
+**Note:** This intent is available
+in Android 4.4 (API level 19) and higher.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER`
-
+:   `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER`
 
 **Data URI**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_LENGTH`
-    :
-        The length of the timer in seconds.
-
+    :   The length of the timer in seconds.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_MESSAGE`
-    :
-        A custom message to identify the timer.
-
+    :   A custom message to identify the timer.
 
     `https://developer.android.com/reference/android/provider/AlarmClock#EXTRA_SKIP_UI`
-    :
-        A boolean specifying whether the responding app must skip its UI when setting the timer. If true, the app must bypass any confirmation UI and start the specified timer.
+    :   A boolean specifying whether the responding app must skip its UI when setting the timer.
+        If true, the app must bypass any confirmation UI and start the specified timer.
 
 **Example intent:**
 
@@ -221,7 +231,8 @@ public void startTimer(String message, int seconds) {
 ```
 **Note:**
 
-To invoke the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER` intent, your app must have the `https://developer.android.com/reference/android/Manifest.permission#SET_ALARM` permission:
+To invoke the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SET_TIMER` intent, your app must have the
+`https://developer.android.com/reference/android/Manifest.permission#SET_ALARM` permission:
 
 ```xml
 <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
@@ -240,26 +251,24 @@ To invoke the `https://developer.android.com/reference/android/provider/AlarmClo
 
 ### Show all alarms
 
-To show the list of alarms, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SHOW_ALARMS` action.
-
-Although not many apps invoke this intent, as it's primarily used by system apps, any app that behaves as an alarm clock can implement this intent filter and respond by showing the list of current alarms.
+To show the list of alarms, use the `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SHOW_ALARMS`
+action.
 
-**Note:** This intent is available in Android 4.4 (API level 19) and higher.
+Although not many apps invoke this intent, as it's primarily used by system apps,
+any app that behaves as an alarm clock can implement
+this intent filter and respond by showing the list of current alarms.
 
+**Note:** This intent is available
+in Android 4.4 (API level 19) and higher.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SHOW_ALARMS`
-
+:   `https://developer.android.com/reference/android/provider/AlarmClock#ACTION_SHOW_ALARMS`
 
 **Data URI**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
+:   None
 
 **Example intent filter:**
 
@@ -274,67 +283,53 @@ Although not many apps invoke this intent, as it's primarily used by system apps
 
 ## Calendar
 
-Adding an event is a common action for calendar apps. Create an intent filter to advertise your app's ability to perform this action using the information in the following section.
+Adding an event is a common action for calendar apps.
+Create an intent filter to advertise your app's ability to perform this action using the
+information in the following section.
 
 ### Add a calendar event
 
-To add a new event to the user's calendar, use the `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT` action and specify the data URI using `https://developer.android.com/reference/android/provider/CalendarContract.Events#CONTENT_URI`. You can then specify various event details using the following extras.
-
+To add a new event to the user's calendar, use the
+`https://developer.android.com/reference/android/content/Intent#ACTION_INSERT`
+action and specify the data URI using
+`https://developer.android.com/reference/android/provider/CalendarContract.Events#CONTENT_URI`.
+You can then specify various event details using the following extras.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT`
 
 **Data URI**
-:
-    `https://developer.android.com/reference/android/provider/CalendarContract.Events#CONTENT_URI`
-
+:   `https://developer.android.com/reference/android/provider/CalendarContract.Events#CONTENT_URI`
 
 **MIME Type**
-:
-    `"vnd.android.cursor.dir/event"`
-
+:   `"vnd.android.cursor.dir/event"`
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/provider/CalendarContract#EXTRA_EVENT_ALL_DAY`
-    :
-        A boolean specifying whether this is an all-day event.
-
+    :   A boolean specifying whether this is an all-day event.
 
     `https://developer.android.com/reference/android/provider/CalendarContract#EXTRA_EVENT_BEGIN_TIME`
-    :
-        The start time of the event (milliseconds since epoch).
-
+    :   The start time of the event (milliseconds since epoch).
 
     `https://developer.android.com/reference/android/provider/CalendarContract#EXTRA_EVENT_END_TIME`
-    :
-        The end time of the event (milliseconds since epoch).
-
+    :   The end time of the event (milliseconds since epoch).
 
     `https://developer.android.com/reference/android/provider/CalendarContract.EventsColumns#TITLE`
-    :
-        The event title.
-
+    :   The event title.
 
     `https://developer.android.com/reference/android/provider/CalendarContract.EventsColumns#DESCRIPTION`
-    :
-        The event description.
-
+    :   The event description.
 
     `https://developer.android.com/reference/android/provider/CalendarContract.EventsColumns#EVENT_LOCATION`
-    :
-        The event location.
-
+    :   The event location.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_EMAIL`
-    :
-        A comma-separated list of email addresses that specify the invitees.
+    :   A comma-separated list of email addresses that specify the invitees.
 
-    Many more event details can be specified using the constants defined in the `https://developer.android.com/reference/android/provider/CalendarContract.EventsColumns` class.
+    Many more event details can be specified using the constants defined in the
+    `https://developer.android.com/reference/android/provider/CalendarContract.EventsColumns` class.
 
 **Example intent:**
 
@@ -385,40 +380,42 @@ public void addEvent(String title, String location, long begin, long end) {
 
 ## Camera
 
-The following are common actions for camera apps, including the information you need to create an intent filter to advertise your app's ability to perform each action.
+The following are common actions for camera apps, including the information you need
+to create an intent filter to advertise your app's ability to perform each action.
 
 ### Capture a picture or video and return it
 
-To open a camera app and receive the resulting photo or video, use the `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE` or `https://developer.android.com/reference/android/provider/MediaStore#ACTION_VIDEO_CAPTURE` action. Also specify the URI location where you'd like the camera to save the photo or video, in the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT` extra.
-
+To open a camera app and receive the resulting photo or video, use the `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE` or `https://developer.android.com/reference/android/provider/MediaStore#ACTION_VIDEO_CAPTURE` action. Also specify the URI location where you'd
+like the camera to save the photo or video, in the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT`
+extra.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE` or  
-    `https://developer.android.com/reference/android/provider/MediaStore#ACTION_VIDEO_CAPTURE`
+:   `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE` or  
 
+    `https://developer.android.com/reference/android/provider/MediaStore#ACTION_VIDEO_CAPTURE`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT`
-    :
-        The URI location where the camera app saves the photo or video file (as a `https://developer.android.com/reference/android/net/Uri` object).
+    :   The URI location where the camera app saves the photo or
+        video file (as a `https://developer.android.com/reference/android/net/Uri` object).
 
-When the camera app successfully returns focus to your activity---in other words, your app receives the `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback---you can access the photo or video at the URI you specified with the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT` value.
+When the camera app successfully returns
+focus to your activity---in other words, your app receives the `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback---you
+can access the photo or video at the URI you specified
+with the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT` value.
 
-**Note:** When you use `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE` to capture a photo, the camera might also return a downscaled copy, or thumbnail, of the photo in the result `https://developer.android.com/reference/android/content/Intent`, saved as a `https://developer.android.com/reference/android/graphics/Bitmap` in an extra field named `"data"`.
+**Note:** When you use `https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE`
+to capture a photo, the camera might also return a
+downscaled copy, or thumbnail, of the photo in the result `https://developer.android.com/reference/android/content/Intent`, saved as a `https://developer.android.com/reference/android/graphics/Bitmap` in an extra field named
+`"data"`.
 
 **Example intent:**
 
@@ -506,7 +503,10 @@ private void dispatchTakePictureIntent() {
 </section></div>
 ```
 
-For more information about how to use this intent to capture a photo, including how to create an appropriate `https://developer.android.com/reference/android/net/Uri` for the output location, read [Take photos](https://developer.android.com/training/camera-deprecated/photobasics) or [Take videos](https://developer.android.com/training/camera-deprecated/videobasics).
+For more information about how to use this intent to capture a photo, including
+how to create an appropriate `https://developer.android.com/reference/android/net/Uri` for the output location, read
+[Take photos](https://developer.android.com/training/camera-deprecated/photobasics) or
+[Take videos](https://developer.android.com/training/camera-deprecated/videobasics).
 
 **Example intent filter:**
 
@@ -519,37 +519,33 @@ For more information about how to use this intent to capture a photo, including
 </activity>
 ```
 
-When handling this intent, have your activity check for the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT` extra in the incoming `https://developer.android.com/reference/android/content/Intent`, then save the captured image or video at the location specified by that extra and call `https://developer.android.com/reference/android/app/Activity#setResult(int, android.content.Intent)` with an `Intent` that includes a compressed thumbnail in an extra named `"data"`.
+
+When handling this intent, have your activity check for the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_OUTPUT` extra in the incoming `https://developer.android.com/reference/android/content/Intent`, then save the captured image or video at the
+location specified by that extra and call `https://developer.android.com/reference/android/app/Activity#setResult(int, android.content.Intent)` with an `Intent` that includes a compressed thumbnail in an extra
+named `"data"`.
 
 ### Start a camera app in still image mode
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "take a picture"
 
 To open a camera app in still image mode, use the `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_STILL_IMAGE_CAMERA` action.
 
-
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_STILL_IMAGE_CAMERA`
-
+:   `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_STILL_IMAGE_CAMERA`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
-:
-    None
+:   None
 
 **Example intent:**
 
@@ -594,31 +590,24 @@ public void capturePhoto(String targetFilename) {
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "record a video"
 
 To open a camera app in video mode, use the `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_VIDEO_CAMERA` action.
 
-
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_VIDEO_CAMERA`
-
+:   `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_VIDEO_CAMERA`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
-:
-    None
+:   None
 
 **Example intent:**
 
@@ -657,30 +646,32 @@ public void capturePhoto() {
 
 ## Contacts/people app
 
-The following are common actions for contacts management apps, including the information you need to create an intent filter to advertise your app's ability to perform each action.
+The following are common actions for contacts management apps, including the information you need
+to create an intent filter to advertise your app's ability to perform each action.
 
 ### Select a contact
 
-To have the user select a contact and provide your app access to all the contact information, use the `https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action and specify the MIME type to `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`.
-
-The result `https://developer.android.com/reference/android/content/Intent` delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback contains the `content:` URI pointing to the selected contact. The response grants your app temporary permissions to read that contact using the [Contacts Provider](https://developer.android.com/guide/topics/providers/contacts-provider) API, even if your app doesn't include the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
+To have the user select a contact and provide your app access to all the contact information,
+use the `https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action and specify the MIME type to
+`https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`.
 
-**Tip:** If you need access to only a specific piece of contact information, such as a phone number or email address, instead see the next section about how to [select specific contact data](https://developer.android.com/guide/components/intents-common#PickContactData).
+The result `https://developer.android.com/reference/android/content/Intent` delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback contains the
+`content:` URI pointing to the selected contact. The response grants
+your app temporary permissions to read that contact using the [Contacts Provider](https://developer.android.com/guide/topics/providers/contacts-provider) API, even if
+your app doesn't include the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
 
+**Tip:** If you need access to only a specific piece of contact
+information, such as a phone number or email address, instead see the next section about how to
+[select specific contact data](https://developer.android.com/guide/components/intents-common#PickContactData).
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_PICK`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_PICK`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`
+:   `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`
 
 **Example intent:**
 
@@ -730,50 +721,61 @@ protected void onActivityResult(int requestCode, int resultCode, Intent data) {
 }
 ```
 
-For information about how to retrieve contact details once you have the contact URI, read [Retrieve details for a contact](https://developer.android.com/training/contacts-provider/retrieve-details).
+For information about how to retrieve contact details once you have the contact URI,
+read [Retrieve details
+for a contact](https://developer.android.com/training/contacts-provider/retrieve-details).
 
-When you retrieve the contact URI using this intent, you generally don't need the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission to read basic details for that contact, such as display name and whether the contact is starred. However, if you're trying to [read more specific data](https://developer.android.com/guide/components/intents-common#PickContactData) about a given contact---such as their phone number or email address---you need the `READ_CONTACTS` permission.
+When you retrieve the contact URI using this intent, you generally don't
+need the
+`https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS`
+permission to read basic details for that contact, such as display name and
+whether the contact is starred. However, if you're trying to
+[read more specific data](https://developer.android.com/guide/components/intents-common#PickContactData) about a given contact---such
+as their phone number or email address---you need the `READ_CONTACTS`
+permission.
 
 ### Select specific contact data
 
-To have the user select a specific piece of information from a contact, such as a phone number, email address, or other data type, use the `https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action and specify the MIME type to one of the following content types, such as `https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds.Phone#CONTENT_TYPE` to get the contact's phone number.
+To have the user select a specific piece of information from a contact, such as
+a phone number, email address, or other data type, use the
+`https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action and specify the MIME type to one
+of the following content types, such as
+`https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds.Phone#CONTENT_TYPE` to get the contact's phone number.
 
-**Note:** In many cases, your app needs to have the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission to view specific information about a particular contact.
+**Note:** In many cases, your app needs to have the
+`https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS`
+permission to view specific information about a particular contact.
 
-If you need to retrieve only one type of data from a contact, this technique with a `CONTENT_TYPE` from the `https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds` classes is more efficient than using the `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`, as shown in the preceding section. The result provides you direct access to the desired data without requiring you to perform a more complex query to [Contacts Provider](https://developer.android.com/guide/topics/providers/contacts-provider).
-
-The result `https://developer.android.com/reference/android/content/Intent` delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback contains the `content:` URI pointing to the selected contact data. The response grants your app temporary permissions to read that contact data even if your app doesn't include the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
+If you need to retrieve only one type of data from a contact, this technique with a
+`CONTENT_TYPE` from the
+`https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds` classes is more efficient than
+using the `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`, as shown in the preceding section. The result provides you direct
+access to the desired data without requiring you to perform a more complex query to [Contacts Provider](https://developer.android.com/guide/topics/providers/contacts-provider).
 
+The result `https://developer.android.com/reference/android/content/Intent` delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` callback contains the
+`content:` URI pointing to the selected contact data. The response grants
+your app temporary permissions to read that contact data even if your app doesn't include the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_PICK`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_PICK`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
 :
 
-
     `https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds.Phone#CONTENT_TYPE`
-    :
-        Pick from contacts with a phone number.
-
+    :   Pick from contacts with a phone number.
 
     `https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds.Email#CONTENT_TYPE`
-    :
-        Pick from contacts with an email address.
-
+    :   Pick from contacts with an email address.
 
     `https://developer.android.com/reference/android/provider/ContactsContract.CommonDataKinds.StructuredPostal#CONTENT_TYPE`
-    :
-        Pick from contacts with a postal address.
+    :   Pick from contacts with a postal address.
 
-    Or one of many other `CONTENT_TYPE` values under `https://developer.android.com/reference/android/provider/ContactsContract`.
+    Or one of many other `CONTENT_TYPE` values
+    under `https://developer.android.com/reference/android/provider/ContactsContract`.
 
 **Example intent:**
 
@@ -845,27 +847,23 @@ protected void onActivityResult(int requestCode, int resultCode, Intent data) {
 
 ### View a contact
 
-To display the details for a known contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW` action and specify the contact with a `content:` URI as the intent data.
+To display the details for a known contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
+action and specify the contact with a `content:` URI as the intent data.
 
 There are two primary ways to initially retrieve the contact's URI:
 
 - Use the contact URI returned by the `https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action shown in the preceding section. This approach doesn't require any app permissions.
-- Access the list of all contacts directly, as described in [Retrieve a list of contacts](https://developer.android.com/training/contacts-provider/retrieve-names). This approach requires the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
-
+- Access the list of all contacts directly, as described in [Retrieve a list of
+  contacts](https://developer.android.com/training/contacts-provider/retrieve-names). This approach requires the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
 
 **Data URI Scheme**
-:
-    `content:<URI>`
-
+:   `content:<URI>`
 
 **MIME Type**
-:
-    None. The type is inferred from the contact URI.
+:   None. The type is inferred from the contact URI.
 
 **Example intent:**
 
@@ -893,32 +891,29 @@ public void viewContact(Uri contactUri) {
 
 ### Edit an existing contact
 
-To edit a known contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_EDIT` action, specify the contact with a `content:` URI as the intent data, and include any known contact information in extras specified by constants in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
+To edit a known contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_EDIT`
+action, specify the contact with a `content:` URI
+as the intent data, and include any known contact information in extras specified by
+constants in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
 
 There are two primary ways to initially retrieve the contact URI:
 
 - Use the contact URI returned by the `https://developer.android.com/reference/android/content/Intent#ACTION_PICK` action shown in the preceding section. This approach doesn't require any app permissions.
-- Access the list of all contacts directly, as described in [Retrieve a list of contacts](https://developer.android.com/training/contacts-provider/retrieve-names). This approach requires the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
-
+- Access the list of all contacts directly, as described in [Retrieve a list of
+  contacts](https://developer.android.com/training/contacts-provider/retrieve-names). This approach requires the `https://developer.android.com/reference/android/Manifest.permission#READ_CONTACTS` permission.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_EDIT`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_EDIT`
 
 **Data URI Scheme**
-:
-    `content:<URI>`
-
+:   `content:<URI>`
 
 **MIME Type**
-:
-    The type is inferred from the contact URI.
-
+:   The type is inferred from the contact URI.
 
 **Extras**
-:
-    One or more of the extras defined in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert` so you can populate fields of the contact details.
+:   One or more of the extras defined in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`
+    so you can populate fields of the contact details.
 
 **Example intent:**
 
@@ -949,31 +944,27 @@ public void editContact(Uri contactUri, String email) {
 }
 ```
 
-For more information about how to edit a contact, read [Modify contacts using intents](https://developer.android.com/training/contacts-provider/modify-data).
+For more information about how to edit a contact, read [Modify
+contacts using intents](https://developer.android.com/training/contacts-provider/modify-data).
 
 ### Insert a contact
 
-To insert a new contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT` action, specify `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE` as the MIME type, and include any known contact information in extras specified by constants in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
-
+To insert a new contact, use the `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT` action,
+specify `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE` as
+the MIME type, and include any known contact information in extras specified by
+constants in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_INSERT`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`
-
+:   `https://developer.android.com/reference/android/provider/ContactsContract.Contacts#CONTENT_TYPE`
 
 **Extras**
-:
-    One or more of the extras defined in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
+:   One or more of the extras defined in `https://developer.android.com/reference/android/provider/ContactsContract.Intents.Insert`.
 
 **Example intent:**
 
@@ -1006,70 +997,61 @@ public void insertContact(String name, String email) {
 }
 ```
 
-For more information about how to insert a contact, read [Modify contacts using intents](https://developer.android.com/training/contacts-provider/modify-data).
+For more information about how to insert a contact, read [Modify
+contacts using intents](https://developer.android.com/training/contacts-provider/modify-data).
 
 ## Email
 
-Composing an email with optional attachments is a common action for email apps. Create an intent filter to advertise your app's ability to perform this action using the information in the following section.
+Composing an email with optional attachments is a common action for email apps.
+Create an intent filter to advertise your app's ability to perform this action using the
+information in the following section.
 
 ### Compose an email with optional attachments
 
-To compose an email, use one of the following actions based on whether you'll include attachments or not, and include email details such as the recipient and subject using the extra keys listed.
-
+To compose an email, use one of the following actions based on whether you'll include attachments or not,
+and include email details such as the recipient and subject using the extra keys listed.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_SENDTO` (for no attachment) or  
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_SENDTO` (for no attachment) or  
+
     `https://developer.android.com/reference/android/content/Intent#ACTION_SEND` (for one attachment) or  
-    `https://developer.android.com/reference/android/content/Intent#ACTION_SEND_MULTIPLE` (for multiple attachments)
 
+    `https://developer.android.com/reference/android/content/Intent#ACTION_SEND_MULTIPLE` (for multiple attachments)
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
 :
 
-
     `"text/plain"`
-
     `"*/*"`
 
-
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/content/Intent#EXTRA_EMAIL`
-    :
-        A string array of all "To" recipient email addresses.
-
+    :   A string array of all "To" recipient email addresses.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_CC`
-    :
-        A string array of all "CC" recipient email addresses.
-
+    :   A string array of all "CC" recipient email addresses.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_BCC`
-    :
-        A string array of all "BCC" recipient email addresses.
-
+    :   A string array of all "BCC" recipient email addresses.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_SUBJECT`
-    :
-        A string with the email subject.
-
+    :   A string with the email subject.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_TEXT`
-    :
-        A string with the body of the email.
-
+    :   A string with the body of the email.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_STREAM`
-    :
-        A `https://developer.android.com/reference/android/net/Uri` pointing to the attachment. If using the `https://developer.android.com/reference/android/content/Intent#ACTION_SEND_MULTIPLE` action, this instead is an `https://developer.android.com/reference/java/util/ArrayList` containing multiple `Uri` objects.
+    :   A `https://developer.android.com/reference/android/net/Uri` pointing to the attachment.
+        If using the
+        `https://developer.android.com/reference/android/content/Intent#ACTION_SEND_MULTIPLE`
+        action, this instead is an
+        `https://developer.android.com/reference/java/util/ArrayList` containing
+        multiple `Uri` objects.
 
 **Example intent:**
 
@@ -1104,7 +1086,9 @@ public void composeEmail(String[] addresses, String subject, Uri attachment) {
 }
 ```
 
-If you want to make sure that your intent is handled only by an email app, and not a text messaging or social app, then use the `https://developer.android.com/reference/android/content/Intent#ACTION_SENDTO` action and include the `"mailto:"` data scheme as shown in the following example:
+If you want to make sure that your intent is handled only by an email app, and not a
+text messaging or social app, then use the `https://developer.android.com/reference/android/content/Intent#ACTION_SENDTO` action
+and include the `"mailto:"` data scheme as shown in the following example:
 
 ### Kotlin
 
@@ -1154,55 +1138,58 @@ public void composeEmail(String[] addresses, String subject) {
 
 ## File storage
 
-The following are common actions for file storage apps, including the information you need to create an intent filter to advertise your app's ability to perform each action.
+The following are common actions for file storage apps, including the information you need
+to create an intent filter to advertise your app's ability to perform each action.
 
 ### Retrieve a specific type of file
 
-To request that the user select a file such as a document or photo and return a reference to your app, use the `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT` action and specify your desired MIME type. The file reference returned to your app is transient to your activity's current lifecycle, so if you want to access it later you must import a copy that you can read later.
+To request that the user select a file such as a document or photo and return a reference to
+your app, use the `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT` action and specify your desired
+MIME type. The file reference returned to your app is transient to your activity's current
+lifecycle, so if you want to access it later you must import a copy that you can read later.
 
-This intent also lets the user create a new file in the process. For example, instead of selecting an existing photo, the user can capture a new photo with the camera.
+This intent also lets the user create a new file in the process. For
+example, instead of selecting an existing photo, the user can capture a new photo with the camera.
 
-The result intent delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` method includes data with a URI pointing to the file. The URI can be anything, such as an `http:` URI, `file:` URI, or `content:` URI. However, if you'd like to restrict selectable files to only those that are accessible from a content provider (a `content:` URI) and that are available as a file stream with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`, add the `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE` category to your intent.
-
-On Android 4.3 (API level 18) and higher, you can also let the user select multiple files by adding `https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE` to the intent, set to `true`. You can then access each of the selected files in a `https://developer.android.com/reference/android/content/ClipData` object returned by `https://developer.android.com/reference/android/content/Intent#getClipData()`.
+The result intent delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` method includes data with a URI pointing to the file.
+The URI can be anything, such as an `http:` URI, `file:` URI, or `content:`
+URI. However, if you'd like to restrict selectable files to only those that are accessible
+from a content provider (a `content:` URI) and that are available as a file stream with
+`https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`,
+add
+the `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE` category to your intent.
 
+On Android 4.3 (API level 18) and higher,
+you can also let the user select multiple files by adding
+`https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE` to the intent, set to `true`.
+You can then access each of the selected files in a `https://developer.android.com/reference/android/content/ClipData`
+object returned by `https://developer.android.com/reference/android/content/Intent#getClipData()`.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    The MIME type corresponding to the file type the user needs to select.
-
+:   The MIME type corresponding to the file type the user needs to select.
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE`
-    :
-        A boolean that declares whether the user can select more than one file at a time.
-
+    :   A boolean that declares whether the user can select more than one file at a time.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_LOCAL_ONLY`
-    :
-        A boolean that declares whether the returned file must be available directly from the device, rather than requiring a download from a remote service.
-
+    :   A boolean that declares whether the returned file must be available directly from
+        the device, rather than requiring a download from a remote service.
 
 **Category** (optional)
 :
 
-
     `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE`
-    :
-        To return only "openable" files that can be represented as a file stream with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`.
+    :   To return only "openable" files that can be represented as a file stream
+        with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`.
 
 **Example intent to get a photo:**
 
@@ -1270,66 +1257,78 @@ protected void onActivityResult(int requestCode, int resultCode, Intent data) {
 
 ### Open a specific type of file
 
-Instead of retrieving a copy of a file that you must import to your app, by using the `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT` action, when running on Android 4.4 or higher you can instead request to *open* a file that's managed by another app by using the `https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` action and specifying a MIME type. To also let the user create a new document that your app can write to, use the `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT` action instead.
-
-For example, instead of selecting from existing PDF documents, the `ACTION_CREATE_DOCUMENT` intent lets users select where they'd like to create a new document, such as within another app that manages the document's storage. Your app then receives the URI location of where it can write the new document.
-
-Whereas the intent delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)` method from the `ACTION_GET_CONTENT` action might return a URI of any type, the result intent from `ACTION_OPEN_DOCUMENT` and `ACTION_CREATE_DOCUMENT` always specify the chosen file as a `content:` URI that's backed by a `https://developer.android.com/reference/android/provider/DocumentsProvider`. You can open the file with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)` and query its details using columns from `https://developer.android.com/reference/android/provider/DocumentsContract.Document`.
-
-The returned URI grants your app long-term read access to the file, also possibly with write access. The `ACTION_OPEN_DOCUMENT` action is particularly useful when you want to read an existing file without making a copy into your app or when you want to open and edit a file in place.
-
-You can also let the user select multiple files by adding `https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE` to the intent, set to `true`. If the user selects just one item, then you can retrieve the item from `https://developer.android.com/reference/android/content/Intent#getData()`. If the user selects more than one item, then `getData()` returns null and you must instead retrieve each item from a `https://developer.android.com/reference/android/content/ClipData` object that is returned by `https://developer.android.com/reference/android/content/Intent#getClipData()`.
-
-**Note:** Your intent **must** specify a MIME type and **must** declare the `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE` category. If appropriate, you can specify more than one MIME type by adding an array of MIME types with the `https://developer.android.com/reference/android/content/Intent#EXTRA_MIME_TYPES` extra---if you do so, you must set the primary MIME type in `https://developer.android.com/reference/android/content/Intent#setType(java.lang.String)` to `"*/*"`.
-
+Instead of retrieving a copy of a file that you must import to your app, by using the `https://developer.android.com/reference/android/content/Intent#ACTION_GET_CONTENT` action, when running on Android
+4.4 or higher you can instead request to *open* a file that's managed by another app by
+using the `https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` action and specifying a MIME type.
+To also let the user create a new document that your app can write to, use the `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT` action instead.
+
+For example, instead of
+selecting from existing PDF documents, the `ACTION_CREATE_DOCUMENT`
+intent lets users select where they'd like to create a new document, such as within another app
+that manages the document's storage. Your app then receives the URI location of where it
+can write the new document.
+
+Whereas the intent delivered to your `https://developer.android.com/reference/android/app/Activity#onActivityResult(int, int, android.content.Intent)`
+method from the `ACTION_GET_CONTENT` action might
+return a URI of any type, the result intent from `ACTION_OPEN_DOCUMENT`
+and `ACTION_CREATE_DOCUMENT` always specify the chosen file as a `content:` URI that's backed by a `https://developer.android.com/reference/android/provider/DocumentsProvider`. You can open the
+file with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)` and
+query its details using columns from `https://developer.android.com/reference/android/provider/DocumentsContract.Document`.
+
+The returned URI grants your app long-term read access to the file, also possibly
+with write access. The `ACTION_OPEN_DOCUMENT` action is
+particularly useful when you want to read an existing file without making a copy into your app
+or when you want to open and edit a file in place.
+
+You can also let the user select multiple files by adding
+`https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE` to the intent, set to `true`.
+If the user selects just one item, then you can retrieve the item from `https://developer.android.com/reference/android/content/Intent#getData()`.
+If the user selects more than one item, then `getData()` returns null and you must instead
+retrieve each item from a `https://developer.android.com/reference/android/content/ClipData`
+object that is returned by `https://developer.android.com/reference/android/content/Intent#getClipData()`.
+
+**Note:** Your intent **must** specify a MIME type and
+**must** declare the `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE` category. If
+appropriate, you can specify more than one MIME type by adding an array of MIME types with the
+`https://developer.android.com/reference/android/content/Intent#EXTRA_MIME_TYPES` extra---if you do so, you must set the
+primary MIME type in `https://developer.android.com/reference/android/content/Intent#setType(java.lang.String)` to `"*/*"`.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` or  
-    `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT`
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` or  
 
+    `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    The MIME type corresponding to the file type the user needs to select.
-
+:   The MIME type corresponding to the file type the user needs to select.
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/content/Intent#EXTRA_MIME_TYPES`
-    :
-        An array of MIME types corresponding to the types of files your app is requesting. When you use this extra, you must set the primary MIME type in `https://developer.android.com/reference/android/content/Intent#setType(java.lang.String)` to `"*/*"`.
-
+    :   An array of MIME types corresponding to the types of files your app is
+        requesting. When you use this extra, you must set the primary MIME type in
+        `https://developer.android.com/reference/android/content/Intent#setType(java.lang.String)` to `"*/*"`.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_ALLOW_MULTIPLE`
-    :
-        A boolean that declares whether the user can select more than one file at a time.
-
+    :   A boolean that declares whether the user can select more than one file at a time.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_TITLE`
-    :
-        For use with `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT` to specify an initial filename.
-
+    :   For use with `https://developer.android.com/reference/android/content/Intent#ACTION_CREATE_DOCUMENT` to specify
+        an initial filename.
 
     `https://developer.android.com/reference/android/content/Intent#EXTRA_LOCAL_ONLY`
-    :
-        A boolean that declares whether the returned file must be available directly from the device, rather than requiring a download from a remote service.
-
+    :   A boolean that declares whether the returned file must be available directly from
+        the device, rather than requiring a download from a remote service.
 
 **Category**
 :
 
-
     `https://developer.android.com/reference/android/content/Intent#CATEGORY_OPENABLE`
-    :
-        To return only "openable" files that can be represented as a file stream with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`.
+    :   To return only "openable" files that can be represented as a file stream
+        with `https://developer.android.com/reference/android/content/ContentResolver#openFileDescriptor(android.net.Uri, java.lang.String)`.
 
 **Example intent to get a photo:**
 
@@ -1379,9 +1378,14 @@ protected void onActivityResult(int requestCode, int resultCode, Intent data) {
 }
 ```
 
-Third-party apps can't respond to an intent with the `https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` action. Instead, the system receives this intent and displays all the files available from various apps in a unified user interface.
+Third-party apps can't respond to an intent with the
+`https://developer.android.com/reference/android/content/Intent#ACTION_OPEN_DOCUMENT` action. Instead, the system receives this
+intent and displays all the files available from various apps in a unified user interface.
 
-To provide your app's files in this UI and let other apps open them, you must implement a `https://developer.android.com/reference/android/provider/DocumentsProvider` and include an intent filter for `https://developer.android.com/reference/android/provider/DocumentsContract#PROVIDER_INTERFACE` (`"android.content.action.DOCUMENTS_PROVIDER"`), as shown in the following example:
+To provide your app's files in this UI and let other apps open them, you must implement
+a `https://developer.android.com/reference/android/provider/DocumentsProvider` and include an intent filter for
+`https://developer.android.com/reference/android/provider/DocumentsContract#PROVIDER_INTERFACE`
+(`"android.content.action.DOCUMENTS_PROVIDER"`), as shown in the following example:
 
 ```xml
 <provider ...
@@ -1394,46 +1398,45 @@ To provide your app's files in this UI and let other apps open them, you must im
 </provider>
 ```
 
-For more information about how to make the files managed by your app openable from other apps, read [Open files using storage access framework](https://developer.android.com/guide/topics/providers/document-provider).
+For more information about how to make the files managed by your app openable from other apps,
+read [Open files using storage access framework](https://developer.android.com/guide/topics/providers/document-provider).
 
 ## Local actions
 
-Calling a car is a common local action. Create an intent filter to advertise your app's ability to perform this action using the information in the following section.
+Calling a car is a common local action. Create an intent filter to advertise your app's
+ability to perform this action using the information in the following section.
 
 ### Call a car
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "get me a taxi"
 - "call me a car"
 
-(Wear OS only)
 
-To call a taxi, use the [`ACTION_RESERVE_TAXI_RESERVATION`](https://developers.google.com/android/reference/com/google/android/gms/actions/ReserveIntents#ACTION_RESERVE_TAXI_RESERVATION) action.
+(Wear OS only)
 
-**Note:** Apps must ask for confirmation from the user before completing this action.
+To call a taxi, use the
+[`ACTION_RESERVE_TAXI_RESERVATION`](https://developers.google.com/android/reference/com/google/android/gms/actions/ReserveIntents#ACTION_RESERVE_TAXI_RESERVATION)
+action.
 
+**Note:** Apps must ask for confirmation from the user
+before completing this action.
 
 **Action**
-:
-    [`ACTION_RESERVE_TAXI_RESERVATION`](https://developer.android.com/android/reference/com/google/android/gms/actions/ReserveIntents#ACTION_RESERVE_TAXI_RESERVATION)
-
+:   [`ACTION_RESERVE_TAXI_RESERVATION`](https://developer.android.com/android/reference/com/google/android/gms/actions/ReserveIntents#ACTION_RESERVE_TAXI_RESERVATION)
 
 **Data URI**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
-:
-    None
+:   None
 
 **Example intent:**
 
@@ -1472,49 +1475,47 @@ public void callCar() {
 
 ## Maps
 
-Showing a location on a map is a common action for map apps. Create an intent filter to advertise your app's ability to perform this action using the information in the following section.
+Showing a location on a map is a common action for map apps.
+Create an intent filter to advertise your app's ability to perform this action using the
+information in the following section.
 
 ### Show a location on a map
 
-To open a map, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW` action and specify the location information in the intent data with one of the following schemes.
-
+To open a map, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW` action and specify
+the location information in the intent data with one of the following schemes.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
 
 **Data URI Scheme**
 :
 
-
     `geo:latitude,longitude`
 
     :   Show the map at the given longitude and latitude. Example: `"geo:47.6,-122.3"`
 
-
     `geo:latitude,longitude?z=zoom`
-    :
-        Show the map at the given longitude and latitude at a certain zoom level. A zoom level of 1 shows the whole Earth, centered at the given *lat* ,*lng* . The highest (closest) zoom level is 23.
+    :   Show the map at the given longitude and latitude at a certain zoom level. A zoom level of
+        1 shows the whole Earth, centered at the given *lat* ,*lng* . The highest
+        (closest) zoom level is 23.
 
         Example: `"geo:47.6,-122.3?z=11"`
 
-
     `geo:0,0?q=lat,lng(label)`
 
     :   Show the map at the given longitude and latitude with a string label. Example: `"geo:0,0?q=34.99,-106.61(Treasure)"`
 
-
     `geo:0,0?q=my+street+address`
 
     :   Show the location for "my street address", which can be a specific address or location query. Example: `"geo:0,0?q=1600+Amphitheatre+Parkway%2C+CA"`
 
-        **Note:** All strings passed in the `geo` URI must be encoded. For example, the string `1st & Pike, Seattle` becomes `1st%20%26%20Pike%2C%20Seattle`. Spaces in the string are encoded with `%20` or replaced with the plus sign (`+`).
-
+        **Note:** All strings passed in the `geo` URI must
+        be encoded. For example, the string `1st & Pike, Seattle` becomes
+        `1st%20%26%20Pike%2C%20Seattle`. Spaces in the string are encoded with
+        `%20` or replaced with the plus sign (`+`).
 
 **MIME Type**
-:
-    None
+:   None
 
 **Example intent:**
 
@@ -1557,41 +1558,31 @@ public void showMap(Uri geoLocation) {
 
 ## Music or video
 
-The following are common actions for music and video apps, including the information you need to create an intent filter to advertise your app's ability to perform each action.
+The following are common actions for music and video apps, including the information you need
+to create an intent filter to advertise your app's ability to perform each action.
 
 ### Play a media file
 
-To play a music file, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW` action and specify the URI location of the file in the intent data.
-
+To play a music file, use the `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW` action and
+specify the URI location of the file in the intent data.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
-
+:   `https://developer.android.com/reference/android/content/Intent#ACTION_VIEW`
 
 **Data URI Scheme**
 :
 
-
     `file:\<URI\>`
-
     `content:\<URI\>`
-
     `http:\<URL\>`
 
-
 **MIME Type**
 :
 
-
     `"audio/*"`
-
     `"application/ogg"`
-
     `"application/x-ogg"`
-
     `"application/itunes"`
-
     Or any other that your app requires.
 
 **Example intent:**
@@ -1638,58 +1629,62 @@ public void playMedia(Uri file) {
 
 [![](https://developer.android.com/static/guide/components/images/voice-icon.png)](https://developers.google.com/voice-actions/system/#system_actions_reference)
 
+
 Google Voice Actions
 
 - "play michael jackson billie jean"
 
-To play music based on a search query, use the `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_MEDIA_PLAY_FROM_SEARCH` intent. An app might fire this intent in response to the user's voice command to play music. The receiving app for this intent performs a search within its inventory to match existing content to the given query and starts playing that content.
-
-In this intent, include the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS` string extra, which specifies the intended search mode. For example, the search mode can specify whether the search is for an artist name or song name.
+To play music based on a search query, use the
+`https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_MEDIA_PLAY_FROM_SEARCH` intent. An app might fire
+this intent in response to the user's voice command to play music. The receiving app for this
+intent performs a search within its inventory to match existing content to the given query and
+starts playing that content.
 
+In this intent, include the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS` string
+extra, which specifies the intended search mode. For example, the search mode can specify whether
+the search is for an artist name or song name.
 
 **Action**
-:
-    `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_MEDIA_PLAY_FROM_SEARCH`
-
+:   `https://developer.android.com/reference/android/provider/MediaStore#INTENT_ACTION_MEDIA_PLAY_FROM_SEARCH`
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    None
-
+:   None
 
 **Extras**
 :
 
-
     `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS` (required)
 
-    :   Indicates the search mode: whether the user is looking for a particular artist, album, song, or playlist. Most search modes take additional extras. For example, if the user is interested in listening to a particular song, the intent might have three additional extras: the song title, the artist, and the album. This intent supports the following search modes for each value of `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS`:
-
+    :   Indicates the search mode: whether the user is looking for a particular artist, album, song,
+        or playlist. Most search modes take additional extras. For example, if the user
+        is interested in listening to a particular song, the intent might have three additional extras:
+        the song title, the artist, and the album. This intent supports the following search modes for
+        each value of `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS`:
 
         *Any* - `"vnd.android.cursor.item/*"`
 
-        :   Play any music. The receiving app plays some music based on a smart choice, such as the last playlist the user listened to.
+        :   Play any music. The receiving app plays some music based on a smart choice, such
+            as the last playlist the user listened to.
 
             Additional extras:
 
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): an empty string. This extra is always provided for backward compatibility. Existing apps that don't know about search modes can process this intent as an unstructured search.
 
-
         *Unstructured* - `"vnd.android.cursor.item/*"`
 
-        :   Play a particular song, album, or genre from an unstructured search query. Apps can generate an intent with this search mode when they can't identify the type of content the user wants to listen to. Use more specific search modes when possible.
+        :   Play a particular song, album, or genre from an unstructured search query. Apps can generate
+            an intent with this search mode when they can't identify the type of content the user wants to
+            listen to. Use more specific search modes when possible.
 
             Additional extras:
 
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): a string that contains any combination of the artist, the album, the song name, or the genre.
 
-
-        *Genre* - `https://developer.android.com/reference/android/provider/MediaStore.Audio.Genres#ENTRY_CONTENT_TYPE`
+        *Genre* -
+        `https://developer.android.com/reference/android/provider/MediaStore.Audio.Genres#ENTRY_CONTENT_TYPE`
 
         :   Play music of a particular genre.
 
@@ -1698,8 +1693,8 @@ In this intent, include the `https://developer.android.com/reference/android/pro
             - `"android.intent.extra.genre"` (required) - The genre.
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): the genre. This extra is always provided for backward compatibility. Existing apps that don't know about search modes can process this intent as an unstructured search.
 
-
-        *Artist* - `https://developer.android.com/reference/android/provider/MediaStore.Audio.Artists#ENTRY_CONTENT_TYPE`
+        *Artist* -
+        `https://developer.android.com/reference/android/provider/MediaStore.Audio.Artists#ENTRY_CONTENT_TYPE`
 
         :   Play music from a particular artist.
 
@@ -1709,8 +1704,8 @@ In this intent, include the `https://developer.android.com/reference/android/pro
             - `"android.intent.extra.genre"`: the genre.
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): a string that contains any combination of the artist or the genre. This extra is always provided for backward compatibility. Existing apps that don't know about search modes can process this intent as an unstructured search.
 
-
-        *Album* - `https://developer.android.com/reference/android/provider/MediaStore.Audio.Albums#ENTRY_CONTENT_TYPE`
+        *Album* -
+        `https://developer.android.com/reference/android/provider/MediaStore.Audio.Albums#ENTRY_CONTENT_TYPE`
 
         :   Play music from a particular album.
 
@@ -1721,7 +1716,6 @@ In this intent, include the `https://developer.android.com/reference/android/pro
             - `"android.intent.extra.genre"`: the genre.
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): a string that contains any combination of the album or the artist. This extra is always provided for backward compatibility. Existing apps that don't know about search modes can process this intent as an unstructured search.
 
-
         *Song* - `"vnd.android.cursor.item/audio"`
 
         :   Play a particular song.
@@ -1734,16 +1728,15 @@ In this intent, include the `https://developer.android.com/reference/android/pro
             - `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_TITLE` (required): the song name.
             - `https://developer.android.com/reference/android/app/SearchManager#QUERY` (required): a string that contains any combination of the album, the artist, the genre, or the title. This extra is always provided for backward compatibility. Existing apps that don't know about search modes can process this intent as an unstructured search.
 
-
         *Playlist* - `https://developer.android.com/reference/android/provider/MediaStore.Audio.Playlists#ENTRY_CONTENT_TYPE`
-
         :
 
             > [!NOTE]
             > Android playlists are deprecated. The API is no longer maintained, but the current functionality remains for compatibility.
 
 
-            Play a particular playlist or a playlist that matches some criteria specified by additional extras.
+            Play a particular playlist or a playlist that matches some criteria specified
+            by additional extras.
 
             Additional extras:
 
@@ -1757,7 +1750,8 @@ In this intent, include the `https://developer.android.com/reference/android/pro
 
 **Example intent:**
 
-If the user wants to listen to music from a particular artist, a search app might generate the following intent:
+If the user wants to listen to music from a particular artist, a search app might generate the
+following intent:
 
 ### Kotlin
 
@@ -1800,7 +1794,12 @@ public void playSearchArtist(String artist) {
 </activity>
 ```
 
-When handling this intent in your activity, check the value of the `https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS` extra in the incoming `https://developer.android.com/reference/android/content/Intent` to determine the search mode. Once your activity has identified the search mode, read the values of the additional extras for that particular search mode. With this information, your app can then perform the search within its inventory to play the content that matches the search query. This is shown in the following example.
+When handling this intent in your activity, check the value of the
+`https://developer.android.com/reference/android/provider/MediaStore#EXTRA_MEDIA_FOCUS` extra in the incoming
+`https://developer.android.com/reference/android/content/Intent` to determine the search mode. Once your activity has identified
+the search mode, read the values of the additional extras for that particular search mode.
+With this information, your app can then perform the search within its inventory to play the
+content that matches the search query. This is shown in the following example.
 
 ### Kotlin
 
@@ -1917,41 +1916,33 @@ protected void onCreate(Bundle savedInstanceState) {
 
 ## New note
 
-Creating a note is a common action for note-taking apps. Create an intent filter to advertise your app's ability to perform this action using the information in the following section.
+Creating a note is a common action for note-taking apps.
+Create an intent filter to advertise your app's ability to perform this action using the
+information in the following section.
 
 ### Create a note
 
-To create a new note, use the [`ACTION_CREATE_NOTE`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#ACTION_CREATE_NOTE) action and specify note details such as the subject and text using following extras.
-
-**Note:** Apps must ask for confirmation from the user before completing this action.
+To create a new note, use the
+[`ACTION_CREATE_NOTE`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#ACTION_CREATE_NOTE) action and specify note details such as the subject and text using following extras.
 
+**Note:** Apps must ask for confirmation from the user
+before completing this action.
 
 **Action**
-:
-    [`ACTION_CREATE_NOTE`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#ACTION_CREATE_NOTE)
-
+:   [`ACTION_CREATE_NOTE`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#ACTION_CREATE_NOTE)
 
 **Data URI Scheme**
-:
-    None
-
+:   None
 
 **MIME Type**
-:
-    [`PLAIN_TEXT_TYPE`](https://developer.android.com/reference/org/apache/http/protocol/HTTP.html#PLAIN_TEXT_TYPE)
-:
-    "\*/\*"
-
+:   [`PLAIN_TEXT_TYPE`](https://developer.android.com/reference/org/apache/http/protocol/HTTP.html#PLAIN_TEXT_TYPE)
+:   "\*/\*"
 
 **Extras**
 :
 
-
     [`EXTRA_NAME`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#EXTRA_NAME)
-    :
-        A string indicating the title or subject of the note.
-
+    :   A string indicating the title or subject of the note.
 
     [`EXTRA_TEXT`](https://developers.google.com/android/reference/com/google/android/gms/actions/NoteIntents#EXTRA_TEXT)
-    :
-        A string indicating the text of the note.
\ No newline at end of file
+    :   A string indicating the text of the note.
\ No newline at end of file
